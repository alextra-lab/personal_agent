# config/model_roles.yaml — role→model assignment (ADR-0099 D1 stage 2, FRE-650;
# ADR-0121 Layer 3; ADR-0145 D4)
#
# The ONE hand-edited home for role→model assignment. There is a single role
# table — `bindings:` below. It used to be split across two top-level keys:
# `roles:` (the original ADR-0099 matrix, `{all: <key>}` shape, read by
# `resolve_role_model_key()`) and `bindings:` (the ADR-0121 Layer-3 table,
# `{deployment: <key>, ...}` shape, read by `resolve_role_target()`). Every
# role in the old `roles:` block already carried the same value as its
# `bindings:` entry, so ADR-0145 D4 deleted `roles:` and repointed
# `resolve_role_model_key()` at `bindings:` too — one table, both resolvers.
#
# `resolve_role_model_key()` (model_loader.py) and `resolve_role_target()`
# (model_loader.py, ADR-0121) both read this `bindings:` block; several
# runtime call sites still call one or the other directly for their role
# (grep `resolve_role_model_key`/`resolve_role_target` under src/). This
# matrix is also read by config_guard's dangling-reference check
# (`check_dangling_model_references`) and shape check (`check_binding_shape`).
#
# ── required_secrets ────────────────────────────────────────────────────────
# Per-profile required AppConfig secret fields (ADR-0099 D4: "required secret
# is per-profile, not global"). Checked at AppConfig startup (safety class) —
# fires only when the ACTIVE profile is the one requiring the secret.
#
# Keyed by AGENT_DEPLOYMENT_PROFILE since FRE-916 phase 2. It was previously
# keyed by which catalog file AGENT_MODEL_CONFIG_PATH selected; that variable is
# gone. `eval` is listed explicitly and requires the same set as `cloud`, which
# is what it enforces today — deriving the key from settings.environment instead
# would have silently dropped it, because APP_ENV=eval maps to DEVELOPMENT.
required_secrets:
  local: []
  cloud:
    - anthropic_api_key
    - openai_api_key
  eval:
    - anthropic_api_key
    - openai_api_key

# ── Layer 3 bindings (ADR-0121) ──────────────────────────────────────────────
# Which deployment each role uses, plus its per-use parameters. These point at
# deployment keys in config/models.yaml's `models:` mapping.
#
# `open: true` marks a role a user may select. Absent means pinned — fail-closed,
# so a role added later is never selectable by omission (ADR-0121 §6). Nothing
# consumes it yet; the guardrail is declared before the mechanism that reads it.
bindings:
  # Owner-directed 2026-08-30: reverted to qwen3.6-35b-thinking. Flash-Next was
  # adjudicated on measured turn wall-clock, not benchmark tok/s: on an ordinary
  # multi-search research turn its context grew 12k -> 83k over six iterations
  # and, at its measured 285 t/s prefill against this model's 923, an 83k call
  # needs ~291s to prefill before emitting a token. The turn hit the 900s
  # orchestrator_task_timeout_seconds deadline (FRE-973) and delivered the
  # degraded stub instead of an answer. The FRE-1317 note that "wall clock is
  # not the only factor" stands — this is that measurement, on real work.
  primary:            { deployment: qwen3.8-flash-next, open: true }
  # No default_timeout override: with a profile active — which is every
  # production request — this role already resolved to the sub_agent
  # deployment's 90s. The 120s only ever appeared on the no-profile path, which
  # raised anyway, and artifact_tools passes its own _draft_timeout_s() at the
  # call site regardless.
  # ADR-0121 T5 (FRE-920): pinned to claude_sonnet — owner-directed 2026-07-20
  # at the master gate (there is only one binding default now that
  # ExecutionProfile is gone, and profile-based redirection no longer
  # applies to either role). artifact_builder's real per-build choice lands
  # with ADR-0122.
  artifact_builder:   { deployment: claude_sonnet, open: true }
  # ADR-0145 D1 (FRE-1445): the local instruct twin (qwen3.8-flash-next-instruct)
  # this role used to bind is deleted — one served model, one catalog entry, no
  # pairing table. `inherit` resolves to whatever the session's primary deployment
  # is, and `mode: worker` asks that deployment for its own non-thinking preset
  # (the exact values the deleted twin carried, now living in each local primary's
  # own `modes:` — see config/models.yaml). This is what makes a primary selection
  # reach the whole turn: choosing a cloud primary gives cloud sub-agents, a local
  # primary gives local ones (previously the sub-agent stayed pinned regardless).
  #
  # `priority: ELEVATED` (ADR-0145 D7, FRE-1449): the role's own rank for the
  # re-homed inference concurrency controller. Read from this binding by the
  # factory (llm_client/factory.py) at the point it builds the client, not
  # hard-coded at the sub-agent dispatch site — otherwise this field is
  # decorative and a second, disagreeing home for the same fact exists.
  sub_agent:
    deployment: inherit
    mode: worker
    max_tokens: 8192  # FRE-1496: committed baseline, worker_report_v1 schema bounds the output
    default_timeout: 600  # FRE-1496: committed baseline, 90s was cutting calls that legitimately run 25-65s
    open: true
    priority: ELEVATED
  entity_extraction:  { deployment: gpt-5.4-mini }
  captains_log:       { deployment: claude_sonnet }
  # ADR-0124 D2 (FRE-947): pinned — no `open: true`. The summariser reads the whole
  # conversation — the user's own text and the assistant's responses — so this
  # binding IS the egress decision, and it is the owner's to make once in
  # configuration rather than a user's to flip. The premise, not the conclusion,
  # was corrected: this comment claimed "full tool payloads unconditionally" until
  # 2026-08-02, which Amendment A (2026-07-23, payloads removed from D2's input)
  # and Amendment B (2026-07-24, summariser is conversation-ONLY — tool metadata
  # removed entirely) had already made false. The binding is still the egress
  # decision because the conversation itself is what egresses.
  session_summary:    { deployment: claude_sonnet }
  insights:           { deployment: claude_sonnet }
  compressor:         { deployment: gpt-5.4-mini }
  # ADR-0138 D1 (FRE-1281): span classification is "a named component, not a regex".
  # Bound to claude_sonnet on MEASURED grounds, not by preference. Scored over the
  # FRE-1281 corpus, 3 samples, 156 document-scorings:
  #   gpt-5.4-mini  11/16 preregistered bars met (recall 0.891, prose_in_fence 0.767)
  #   claude_sonnet 15/16                        (recall 0.953, prose_in_fence 1.000)
  # gpt-5.4-mini cannot be improved in place: reasoning_effort above `none` is an
  # OUTAGE on that deployment, not a cost change (litellm rejects it with the
  # `temperature: 0.0` FRE-758 pin), and entity_extraction and compressor bind there
  # too. So this is a model choice, not a tuning knob. The contract's strength is
  # bounded by extraction recall, which is why the stronger model is the right default
  # here and why the cheaper one is a deliberate downgrade rather than a saving.
  # Reproduce either figure with harness.py --model-key.
  span_extraction:    { deployment: claude_sonnet }
  # ADR-0138 D3(d) (FRE-1286): the entailment judge. Bound to claude_sonnet because the
  # ADR names the failure mode directly when rejecting Option 5 — "requires the
  # verification model to be at least as capable as the primary, or it becomes the new
  # weakest link". A judge weaker than the model it audits does not audit it. Its own
  # error rate is measured against a labelled corpus rather than assumed
  # (scripts/eval/fre1286_entailment), which is the other half of that rejection.
  # FRE-1286: pinned — no `open: true`. The judge decides whether an assertion may be
  # delivered, so binding it is a correctness decision rather than a user preference.
  entailment:         { deployment: claude_sonnet }
  embedding:          { deployment: embedding }
  reranker:           { deployment: reranker }
  reranker_fallback:  { deployment: reranker_fallback }
  # ADR-0121 T5 (FRE-920): pinned — no user control (ADR-0102 model-coupling).
  # Resolved directly by _resolve_vision_routing_key/_resolve_document_routing_key
  # (orchestrator/executor.py) for any turn carrying a raster image or a
  # Tier-2 PDF, replacing the old profile-escalation logic.
  vision:             { deployment: claude_sonnet }
