# Last session — 2026-07-13

## Doing / discussing  (≤5 sentences)
A long, high-throughput master-gate session. Shipped: the **ADR-0115 corpus de-noise ops** (FRE-865 backfill + FRE-868 eviction ran against prod → Core **7,997→6,625, all World/Personal, 0 System**); the **vision docs/PDF chain** T2/T3 (FRE-682/683 merged, 684/T4 now building on build1); **FRE-632 owner-identity unification** (folded the two split "Alex" nodes into one `:Person:Entity`, prod migration + gateway rebuild) and **FRE-869 cost-fix**, both **deployed live via gateway rebuild to `7131c011`**. The spine was the **ADR-0116 channel / event-driven-dispatch track**: merged the ADR (FRE-852 Done), then **FRE-871 where master ran the LIVE headless-channel AC-2 proof — PROVEN** (the whole approach is de-risked), then **consolidated** the remaining chain. At reset: build2 building the consolidated **FRE-872** (channel wiring), build1 on vision T4, and the **channel cutover (FRE-875) deliberately deferred to a fresh master** with full context headroom.

## Commits — the story behind the last 10
- **#512 / d980f638 (FRE-871)** — ADR-0116 Phase-1 channel artifacts. Merged inert (default-off). AC-2 was **not** in the PR by the owner's *build-then-master-verifies* decision; master proved it live post-merge (below), then reverted the test's system changes.
- **#511 / d138ba1b (FRE-739 user_id logs)** — **bounced first**: a Standard multi-file src change that shipped with **no codex plan-review**, mis-tiered off the ticket's own "mechanical propagation" self-label. build2 only retriggered CI (a transient CodeQL Actions outage — the `3b25e70c` chore); master then **ran codex itself** (clean, no blocking) to close the tier gate rather than bounce-loop. Merged.
- **#510 / 6f5aff44** — docs: gateway rebuilt to `7131c011`, 632+869 deployed live.
- **#509 / 30aae373+f56c0700 (FRE-632)** — owner-identity unify; code-review found 5 real defects (success-gate, self-loop guard, health-metric filter) fixed on-branch. Re-tiered to **Opus** at dispatch (design decision + prod migration).
- **#508 (FRE-683 T3)** — document-resolution module; code-review offloaded CPU-bound pdfium work to `asyncio.to_thread`.

## Worktrees — anything special
- **build2 — building the consolidated FRE-872** (channel delivery end-to-end, `[codex: required]`). Poked with the re-scope right after it started, so it should plan the full scope.
- **build1 — vision chain**, FRE-684/T4 (capability routing + turn-assembly wiring); `context:keep` was set (shares 683's module).
- **`telemetry/` holds two retained undo records** — `fre632_alex_backup_*` (owner-node neighborhood pre-fold) and `fre868_eviction_snapshot_*` (126 MB). **Keep them** — they're the rollback data for the two prod ops; don't commit them.

## Plan position + drift
On-plan across priority #1 (vision docs) + #2 (memory: 865/868 de-noise, 632 identity, 866 structural-arm wiring) + the channel-automation track. MASTER_PLAN synced this reset. **One deliberate structural change:** the ADR-0116 chain was **consolidated mid-flight** — FRE-873/874 Canceled into FRE-872 — on the principle *decompose finely when unproven, collapse once the load-bearing risk is retired* (AC-2 proved it). Same instinct to apply to the vision chain as it matures.

## Answers for the fresh start
- **FRE-871 AC-2 is PROVEN LIVE (the big result).** Headless channel-push works on this individual Claude Max account. **Risk row 1 RESOLVED** — the individual account *does* honor a custom `allowedChannelPlugins`; no dev-flag fallback needed. The missing enablement step was **`claude plugin install`** (marketplace-add + allowlist alone left the channel dark). Headless RC **auto-dismisses** new-MCP dialogs, so the cutover launcher must set **`enableAllProjectMcpServers`** or workers lose their MCP tools. Full detail on FRE-871's comment + memory `reference_cc_channel_enablement_and_headless_dialogs`.
- **Channel chain is now 2 tickets, not 4:** **FRE-872** = full end-to-end wiring (ledger transport + per-seat mode + gateway channel push + fallback + boundary; AC-1/3/4/5/6; `[codex: required]`), building on build2. **FRE-875** = the **cutover** (ask-first deploy: managed-settings write + per-seat flip + delete the idle-scrape). Reset was taken **before** the cutover on purpose — 875 is a heavy live op that wants a rested master (like AC-2 was today).
- **FRE-739** is Awaiting Deploy (batches with the next gateway rebuild). It's the **ADR-0107 seam owner** — the assembled AC-1/2/3a/3b/4/5 verification needs **FRE-740** merged + one live non-owner request. Codex flagged 2 non-blocking `user_id` coverage gaps (`/chat/stream` pre-bind logs, post-scope consolidator summary logs) — check at the seam.
- **Config-guard is now a REQUIRED merge check** (owner made the ruleset UI change — master's PAT lacks Administration scope, see memory). **FRE-876** (ADR-0099 D4 field-self-documentation check) filed Approved-but-parked, ready to slot when a lane frees.
- **Deploy state:** gateway at `7131c011`. FRE-739, the vision chain, and FRE-866 (structural arm, flag-dark) are all inert-Awaiting-Deploy — they bake with the next gateway rebuild (likely the 875 cutover or the vision T9 seam).
- **Lesson applied:** don't trust a ticket's "mechanical/not-design" self-label for a multi-file src change — re-tier or add `[codex: required]` at dispatch (memory `feedback_ticket_mechanical_selflabel_causes_codex_skip`; already applied to the consolidated FRE-872).
