# Master Plan — Personal Agent

> **Source of truth for work items**: [Linear (FrenchForest)](https://linear.app/frenchforest)
> **Source of truth for priorities**: This file
> **Last updated**: 2026-05-28 — **FRE-410 follow-up (PR #100, deployed) + Cloudflare SSE-buffering fix verified + FRE-412/413 filed**: (1) **PR #100** — `read`'s truncation marker + tool description rewritten into plain, imperative grep-first guidance (FIND `grep -n` → READ `offset`/`limit` → REPEAT → answer; embeds the resolved path; states unseen-line count) so a weaker model pages instead of answering from the head; verified live (local SLM grep'd FSM symbols then paged `executor.py` offset=1095 + `loop_gate.py` offset=195 → correct two-FSM answer from targeted reads, not a 145K-char slurp). (2) **Cloudflare SSE-buffering root cause + fix** (inference-server router, user side): long `artifact_draft` generations were 524ing / re-dispatched to the origin every ~2 min (N× wasted generation, invisible from the gateway since `LocalLLMClient` already streams `stream=True`) because the SLM's streaming response lacked `Cache-Control: no-cache` + `Content-Type: text/event-stream`, so Cloudflare buffered it and re-applied its ~100s origin timeout. Header fix verified end-to-end: chunks arrive live, one generation, no re-dispatch, **493s → 91s**, 23 KB artifact, zero real errors. (3) Filed **FRE-412** (entity dedup over-merges distinct concepts — 0.85 cosine threshold, embedding-only, no type/name guard; collapsed `LLM_CALL`/`TOOL_EXECUTION`/`SYNTHESIS` → `model_call_error`) and **FRE-413** (decouple `artifact_draft` into an async job that appends the artifact URL when ready; URL-driven `ArtifactCard` per ADR-0070 Tier 3; aligns with FRE-401) — both Needs Approval. **FRE-411 in-flight** (PR #101, concurrent CC session): SLM trace identity headers + Caddyfile ES route — the observability gap that hid the origin amplification this session. Previously: **FRE-410 ✅ shipped (PR #99, deployed)**: `read` primitive now defaults to a truncated head (first 200 lines / 8 KB) with `truncated=true` + a continuation `marker`, plus 1-based `offset`/`limit` paging. Byte cap enforced on whole-line boundaries so paging never loses/duplicates content; a single oversized line is clipped with a marker pointing to a larger `max_bytes`. `too_large` error dropped (no caller depended on it — registry + tests only); `tail_lines` keeps its 1 MiB legacy cap (no log-read regression); `session_id` threaded alongside `trace_id` on every log site (ADR-0074). The cheap, high-leverage half of FRE-401. Codex-reviewed twice (pre-impl plan + impl diff) — two boundary bugs (oversized-single-line marker, non-positive `limit`) found and fixed pre-merge. **Live verification on the exact incident file**: reading `orchestrator/executor.py` (133,969 bytes ≈ 33K tokens) now returns 7,204 bytes ≈ ~1.8K tokens (200 of 3,335 lines) with a correct marker — a ~31K-token single-read reduction (AC#5). 2765 tests pass; mypy + ruff + pre-commit clean; gateway rebuilt + healthy. Previously: **turn-forensics skill + 2 follow-up tickets filed**: added `docs/skills/turn-forensics.md` (committed to main; auto-loads on next gateway rebuild) — cross-substrate trace reconstruction + context-token attribution (gateway logs + Postgres session_events + R2 + SLM log, joined on trace_id). Born from diagnosing why a turn's context jumped to ~57K (a single whole-file `read` of executor.py ≈ 33K tokens). Filed **FRE-410** (High, Tier-2, Needs Approval): `read` per-read token cap + `offset`/`limit` ranged reads — the cheap half of FRE-401. Filed **FRE-411** (Medium, Tier-2, Needs Approval): SLM request telemetry joinable by trace_id (harness `X-Trace-Id` header + slm_server structured `request_complete` log w/ prefill/decode split + KV-cache reuse → ES `slm-requests-*`); slm_server side owned by a separate CC, interface contract pinned in the ticket. Previously: **artifact_draft timeout → reasoning-model budget (PR #98, deployed)**: `artifact_draft`'s HTML sub-agent was hard-capped at 120s; now `_draft_timeout_s()` resolves the active config's `primary` (reasoning model) `default_timeout` (600s in `config/models.yaml`) so a full-document generation gets the same budget as a reasoning call and tracks it (no drift). Only `artifact_draft` widened; other sub_agent uses keep fail-fast 90s. Deployed-image verified `_draft_timeout_s()`=600. Pairs with the user raising the SLM server's sub-agent `default_timeout` to 600s. FRE-402's terminal short-circuit still fires cleanly at the (longer) cap. Previously: **FRE-402 extended (PR #97, deployed)**: `artifact_draft`'s `<script>`/inline-handler validation rejection now raises `TerminalToolError` (was plain `ToolExecutionError` → looped back to the reasoning model). The local SLM reliably re-emits scripts when asked for interactivity (Tailwind CDN / inline JS), so a retry just burned another multi-minute reasoning + ~109s generation round-trip (observed live, trace `cd5571fd`). Sandbox violations (script tags, inline handlers) → terminal short-circuit; malformation (too small / missing DOCTYPE / closing tag) stays recoverable. Deployed-image behavior verified (script→terminal w/ reason+next_step, doctype→recoverable). FRE-402 → Done (covers timeout **and** validation rejection). Note: a separate UX gap surfaced — `turn_status` is only emitted *after* the first model call completes (executor.py:2267), so a turn whose first call 524s shows no status bar at all; worth a follow-up (emit an initial `turn_status` at turn start). Previously: **FRE-404 P0 In Review (PR #96)**: corpus renderer (`scripts/render_prompt_corpus.py`) + unified token counter (`src/personal_agent/llm_client/token_counter.py`, tiktoken cl100k_base); `make render-prompt-corpus` → `docs/reference/PROMPT_CORPUS.md` (107KB, all 13 leaf prompts rendered with token counts). Previously: **FRE-402 ✅ shipped (PR #95, deployed)**: terminal tool failures short-circuit the reasoning loop. A non-recoverable tool error (today: `artifact_draft` sub-agent timeout) no longer routes back through the primary model (which had spent a full ~3.5-min call to explain a known failure, trace `b8cd1d53`). Tools self-declare via `TerminalToolError(reason, next_step)` → `ToolResult.metadata` → `step_tool_execution` short-circuits to FAILED with classified `tool_failure` + deterministic reply, reusing the shipped FRE-398 `RUN_ERROR` emit path; no recovery LLM round-trip. `tool_failure` category added to backend Literals + PWA `ClassifiedErrorCard`. Only artifact_draft timeout is terminal; other tool errors still loop back for model recovery (self-describing, no executor allowlist). Gateway rebuilt; deployed-image behavior verified (layer maps TerminalToolError→metadata.terminal). FRE-402 → Done. Previously: **Prompt Management & Observability initiative queued (ADR-0078)**: FRE-267 (per-session thumbs) canceled and superseded; FRE-403 EPIC + FRE-404/405/406/407/408/409 (P0–P5) filed (all Needs Approval). ADR + spec pushed to main. Previously: **FRE-396 ✅ shipped (PR #94, deployed)**: Mermaid diagrams rendered to static inline SVG via mmdc in artifact_draft; `<script>` ban intact; ADR-0070 D7 amended. Previously: **FRE-401 filed (Needs Approval, Tier-1:Opus)**: planner-executor split design thread — reasoning model plans, subagents execute with isolated context; motivated by 507s 524 on 27-message context (trace `2b6fb19c`). Also: scroll fix for DecisionCard/ClassifiedErrorCard (direct-to-main). Previously: **FRE-398 fully shipped (PR #92 + PR #93)**: classified turn failures + partial-work salvage + RUN_ERROR transport event + ClassifiedErrorCard PWA rendering. FRE-398 → Done. Previously: **4 design threads opened (FRE-397/398/399/400)** from the FRE-389 review + dynamic-artifacts/524-timeout discussions: Dynamic artifacts (FRE-397), Bubble-up errors (FRE-398, High), Adaptive limits & error recovery (FRE-399), E2E testing (FRE-400) — all Needs Approval; see **§Active Design Threads** for scope + recommended order. Also filed earlier: FRE-396 (Mermaid→SVG artifacts), FRE-394 (PWA SW dead code), FRE-395 (PWA ESLint). Previous: **Session wrap — FRE-389 on-device polish + 2 follow-ups filed.** FRE-389 PWA walkthrough: `TurnStatusBar` made persistent + moved to the footer with the input controls (PR #90), then tighter iPhone-portrait bottom margin (PR #91) — both deployed; user confirmed the status bar (AC-8). **FRE-389 still In Progress** — remaining: on-device DecisionCard/Stop walkthrough (trigger tool-iteration-limit pause → Continue/Finish, Stop button, amber/red thresholds, compression pause) → then Done. New follow-ups (both **Needs Approval**): **FRE-394** — PWA `sw.js` is dead code (never registered; CACHE_NAME ritual is a no-op; cause of the stale-shell-on-resume confusion) → recommend wiring registration + auto-update; **FRE-395** — no ESLint config on `seshat-pwa` (only type-check gates the frontend) → add flat config + lint gate. **Needs-Approval queue: FRE-394, FRE-395** (+ FRE-381 post-gate). Previous: **FRE-393 ✅ shipped (PRs #88 + #89, deployed) — identity-threading gate armed**: ADR-0074 joinability gate was config-only and never enforced (pre-commit hook never installed; `core.hooksPath` redundantly set to git's default blocked install → 73 unattributed log sites accumulated; surfaced via FRE-389 joinability-probe red). Reworked `check_identity_threaded.py` to **scope-aware deny-by-default** (Codex-reviewed Option D): a log must carry `trace_id` when reachable (incl. `ctx`/`trace_ctx` carriers), else `session_id` in connection/lifespan scope, else exempt (boot) — self-describing, no hardcoded name table; new request-path logs flagged by default. Threaded real identity into genuine gaps (executor constraint helpers, app.py chat dedup, ws_ticket, ws_endpoint waiter-resolve, captains_log feedback). Allowlist 70+→8 (background/FP). Greened sibling hooks: scrubbed 6 historical docs to relative paths, genericized executor prod-prompt host paths (no deployment-specific host paths in public repo), fixed substrate exemption line; checker self-exclude. **`pre-commit install` done + verified firing** (unset redundant `core.hooksPath`, owner-approved). 2687 tests pass; gate exits 0. Gateway redeployed. Previous: **FRE-389 PR2 frontend shipped (PR #87, deployed)**: ADR-0076 constraint governance Phase 1, frontend half. `DecisionCard` (inline non-blocking card: option buttons via action_id→label map, countdown bar, remember toggle), `TurnStatusBar` (replaces `ContextBudgetMeter` — live ctx/tools/cost with amber/red thresholds), Send→Stop button (USER_CANCEL), resolved/cancelled pills; `useSSEStream` turn_status + CONSTRAINT_PAUSE/RESOLVED/CANCELLED handlers + senders. `npm run build` green; `seshat-pwa` rebuilt + healthy, sw CACHE_NAME → v6. **FRE-389 stays In Progress pending on-device UI verification** (AC-1/2/3/4/8/9/10/11/12/16 — backend pause events live on prod). Then → Done. Previous: **FRE-389 PR1 backend shipped (PR #86, deployed + verified)**: ADR-0076 constraint governance Phase 1, backend half. `ConstraintPauseEvent`/`ConstraintResolvedEvent`/`CancelledEvent` + adapter mappings; action-ID registry; WS `WaiterMetadata`/`ConstraintDecision`/`USER_CANCEL` + decision validation; **waiter registration race fixed** (register-before-push via `on_registered`); executor `_maybe_pause_for_constraint` wired into tool-iteration limit (continue_10 extends +10 / finish_now synthesizes) + hard compression (stop_here synthesizes); USER_CANCEL synthesis checkpoint; live `turn_status` STATE_DELTA (context/tools/cost) + `turn_cost_usd` accumulation; `user_constraint_preferences` table (migration 0006 applied to prod) + `PUT /api/v1/preferences/constraint`. 18 new tests; 2682 pass; mypy + ruff(src) clean. Deployed to seshat-gateway, /health green, endpoint live (401 unauth), modules import in-container. **FRE-389 stays In Progress** — frontend PR2 (DecisionCard, TurnStatusBar, Send→Stop) pending. Note: joinability probe red is **pre-existing** (agent_logs missing trace_id on a session that predates deploy — same root cause as the 73 pre-existing identity-threading violations on main; my new emit sites carry the identity tuple, 3 WS-layer logs allowlisted). Pre-existing identity/joinability debt warrants a separate cleanup ticket. Previous: **FRE-392 ✅ shipped (PR #85)**: WS duplicate message guard — `MessageDeduplicator` with client_msg_id + content hash, 120s TTL, release-on-completion, 15 tests. FRE-389/391 approved; ADR-0076 Codex-reviewed (3 passes, all blockers resolved). ADR README synced (ADR-0075 Implemented, ADR-0077 added). Previous: **ADR-0077 artifact_draft shipped (PR #84, verified live)**: Plan/generate split for HTML artifacts — primary model plans, sub-agent (Qwen3.6 instruct / Haiku) generates HTML, executor chains to artifact_write. Profile-driven model selection, full ADR-0074 joinability (child spans, trace_ctx threading), HTML output validation (ADR-0070 D7 sandbox). Fixed stale local.yaml profile refs + bumped sub_agent context_length 16384→32768. E2e verified: sub-agent generated 17.9KB HTML in 97s from 4K-char plan, full trace chain in ES. FRE-392 filed (WS duplicate delivery found during verification). Previous: **FRE-388 ✅ shipped (PR #83 + 8 hotfixes, verified live on iPad)**: SSE→WebSocket transport (ADR-0075). Postgres `session_events` replay buffer, single-use WS ticket auth, per-connection decision waiter registry. PWA native WS client with reconnect + `last_seq` persistence. Post-merge fixes: TS strict-mode cast, `CAST()` vs `::jsonb`, Caddy `/ws/*` routing (snippet matcher bug — required dedicated handle block in site), CORS + WS origin allowlists, WS-before-sendMessage ordering, DONE persistence, fire-and-forget→sync Postgres for dedup, concurrent read conflict (Codex), client-side seq dedup. `claude_sonnet.max_tokens` 8192→32768 applied to `models.cloud.yaml`. FRE-390 filed (eval transport coverage gap, Low). FRE-391 filed (dynamic max_tokens per tool/task, Medium). FRE-389 (ADR-0076 constraint governance) unblocked. Previous: **FRE-388/389 created (Needs Approval)**: ADR-0075 (WS transport) + ADR-0076 (adaptive constraint governance) authored and Codex-reviewed. Immediate fixes: `claude_sonnet.max_tokens` 8192→32768, `default_timeout` 60→180, `RESERVATION_TTL_SECONDS` 90→180; gateway rebuilt. ADR README index updated (was missing ADR-0054 through ADR-0076). Previous: **FRE-385 24h gate PASS**: 3 CL-*.json files from 3 real interactions since deploy (May 26 20:25, May 27 04:28, May 27 04:36 UTC). All well-formed with full reflection structure (rationale, proposed_change, metrics_structured, telemetry_refs). Backfill healthy, zero errors. 2-week promotion gate still running (earliest ~2026-06-09). Previous: **FRE-328 gate reset**: eval-polluted dataset discarded, agent-noun taxonomy deployed (`{domain}-{noun}`), new review gate 2026-06-09. Previous: **FRE-387 ✅ shipped PR #82**. Previous: **FRE-385 deployed 19:57 UTC (PR #81)**. Previous: **`c271c60` feedback-loop fixes: eval isolation (`channel=EVAL` → `eval_mode` in TraceContext), mandatory dedup gate, user attribution in agent-filed issues, stronger tool description, feedback loop guide updated. Ticket triage: FRE-357/358/382 canceled, FRE-383/384/360/361 adjusted. New tickets: FRE-385 (CL revival, Urgent, Approved), FRE-387 (eval isolation extension, High, Approved). FRE-386 canceled (wrong design — "Needs Approval" Linear state is the gate). Rebuilt + deployed.** Previous: **PR #81 (`a79fd7b`) hotfix: thread session_id to all remaining LLM call sites** (sub_agent x2, expansion_controller x3, expansion.py, route_skills, compress_turns/summarize_middle, captains_log reflection — 8 files, 7 sources). Eliminates `cost_record_missing_identity` on user-facing paths; deployed 2026-05-24 20:29 UTC, confirmed live in container. 2611 tests pass, mypy + ruff clean. **FRE-328 gate opened today (2026-05-26)** — naming-stability review window is now open; see `docs/plans/fre-328-evaluation-window.md`. **FRE-350 gate opened 2026-05-24** — reflection-surfacing eval now startable. Previous session (2026-05-24): PR #74 `cd82179` (FRE-376 Phase 5), PR #75 `c369cf0` (entity_extraction trace_ctx regression), PR #76 `0770d41` (P0 cross-user data leak hotfix), PR #77 `aa30cf5` (6 sibling trace_ctx sites), PR #78 `9d44610` (FRE-380 Stage 1 stub-Turn cap), PR #79 `d38f5e1` (FRE-378 /approval session-id mismatch), PR #80 `6b9a824` (FRE-379 /api/v1/knowledge/* user scoping). **FRE-376 In Progress** — 7-day gate closes 2026-05-30; reminder routine fires 09:00 UTC. **Needs-Approval queue**: FRE-381 (Stage 2 consolidator decouple, post-gate). **Wave H next**: 7-day gate → FRE-374 purge→replay → FRE-381 Stage 2 → FRE-178/179/180.

---

## Current State

Waves A ✅ B ✅ C ✅ E (FRE-213, FRE-227, FRE-368) ✅ J ✅ complete. ADR-0063 (primitive tools) fully closed with FRE-265 legacy-tools deletion. ADR-0068 self-telemetry chain fully shipped (PRs #30–33). FRE-326 closed 2026-05-14 (host-resource gates retained deployment-conditional). **Artifact substrate** shipped 2026-05-17 (PR #65). **FRE-368** fully shipped 2026-05-21 (PRs #66 + #67): artifact_write/list/read tools, public listing + telemetry endpoints, PWA inline cards + /artifacts route, sandboxed viewer, all ACs verified live on iPad. **Next**: FRE-369 (user uploads). Gates: FRE-350 post-deploy eval ≥ 2026-05-24; FRE-328 naming-stability ≥ 2026-05-26; ADR-0070 D8 review ≥ 2026-06-04.

Wave D implementation (endpoint abstraction, compose unification, test parity) remains deferred per FRE-214 audit §8.7 until further owner direction.

---

## Active Design Threads (opened 2026-05-28)

Four focused threads carved out of the FRE-389 on-device review + the dynamic-artifacts / 524-timeout discussions. All **Needs Approval**; intended to be worked through one at a time. Each thread issue captures the problem, the ideas surfaced, draft ACs, and open questions.

| Thread | Issue | Scope | Children / related |
|--------|-------|-------|--------------------|
| **Dynamic artifacts** | [FRE-397](https://linear.app/frenchforest/issue/FRE-397) (Tier-1:Opus) | Diagrams now → interactive later (Tier 1 SVG → Tier 2 sandboxed JS → Tier 3 JSX). Key: `artifacts.frenchforet.com` is already an isolated origin, so Tier 2 is a policy flip, not infra. | [FRE-396](https://linear.app/frenchforest/issue/FRE-396) (Tier 1 Mermaid→SVG); ADR-0070/0069/0077 |
| **Bubble up useful errors** | [FRE-398](https://linear.app/frenchforest/issue/FRE-398) (Tier-2:Sonnet, **High**) | Classify failures → actionable user message + next step; don't discard a turn's work on a model-call error. | FRE-389 (DecisionCard pattern) |
| **Adaptive limits & error recovery** | [FRE-399](https://linear.app/frenchforest/issue/FRE-399) (Tier-1:Opus) | Retry/fallback on transient 5xx/524; local inference-server telemetry → pre-flight/adaptive; dynamic thresholds (beyond static walls). | FRE-391 (dynamic max_tokens), FRE-389, `brainstem/sensors/*` |
| **E2E testing (transport/UI/error)** | [FRE-400](https://linear.app/frenchforest/issue/FRE-400) (Tier-2:Sonnet) | Automated WS round-trip + PWA + failure-path coverage for the above (the manual phone walkthroughs today). | FRE-390 (transport eval gap) |
| **Planner-executor split** | [FRE-401](https://linear.app/frenchforest/issue/FRE-401) (Tier-1:Opus) | Reasoning model produces a structured plan; subagents execute with isolated context — no fat conversation history on inference calls. ADR required before implementation. | ADR-0077 (prototype), FRE-399, FRE-391 |

**Recommended order** (mine; owner picks): **FRE-398** (cheap, fixes the pain just hit) → **FRE-396** (unblocks the diagram-artifact use case) → **FRE-399** Layer 2 retry/fallback (quick win) → **FRE-400** (so the rest stop needing manual verification) → FRE-399 Layer 3 telemetry + FRE-397 Tier 2 (decision-gated, ADR work).

**Also open / standalone (not part of the four threads):** FRE-394 (PWA SW dead code), FRE-395 (PWA ESLint). **FRE-389** (constraint governance) remains In Progress pending the on-device DecisionCard/Stop walkthrough.

---

## Upcoming — Wave Sequence

| Wave | Theme | Status | Key Issues | Notes |
|------|-------|--------|------------|-------|
| **A** ✅ | Dev loop & hygiene | Done | FRE-309 · FRE-185/189/320/321/312/308 | All shipped 2026-05-08 |
| **B** ✅ | Self-observation | Done | FRE-301 ✅ · FRE-300 ✅ · FRE-319 ✅ · FRE-269 ✅ · [FRE-326](https://linear.app/frenchforest/issue/FRE-326) ✅ | FRE-326 closed 2026-05-14 — gates retained as deployment-conditional |
| **C** ✅ | Security | Done | [FRE-225](https://linear.app/frenchforest/issue/FRE-225) ✅ | Shipped 2026-05-08 |
| **D** | Architecture | Planning ✅, impl deferred | [FRE-214](https://linear.app/frenchforest/issue/FRE-214) ✅ · FRE-238 · FRE-240 · FRE-241 · FRE-236 · FRE-336 · FRE-338–340 · [FRE-341](https://linear.app/frenchforest/issue/FRE-341) ✅ | Tracks 2a/2b/3 + follow-ups unblocked; deferred per audit §8.7; FRE-341 shipped 2026-05-13 |
| **E** ✅ (partial) | Identity & write surface | Substrate + artifacts shipped | [FRE-213](https://linear.app/frenchforest/issue/FRE-213) ✅ · [FRE-227](https://linear.app/frenchforest/issue/FRE-227) ✅ · [FRE-371](https://linear.app/frenchforest/issue/FRE-371) ✅ · [FRE-368](https://linear.app/frenchforest/issue/FRE-368) ✅ · [FRE-369](https://linear.app/frenchforest/issue/FRE-369) (uploads, Approved) · [FRE-342](https://linear.app/frenchforest/issue/FRE-342) ✅ · [FRE-343](https://linear.app/frenchforest/issue/FRE-343) · [FRE-344](https://linear.app/frenchforest/issue/FRE-344) ✅ · FRE-345 | FRE-368 shipped 2026-05-21. **ADR-0077 artifact_draft shipped 2026-05-27 (PR #84)** — sub-agent HTML generation, verified live. FRE-369 next. |
| **F** | Self-improvement | Partial | [FRE-328](https://linear.app/frenchforest/issue/FRE-328) 🅿️ · [FRE-385](https://linear.app/frenchforest/issue/FRE-385) ✅ · [FRE-387](https://linear.app/frenchforest/issue/FRE-387) ✅ · FRE-226 · FRE-234 | FRE-328 pipeline shipped (PRs #43–47), parked 2026-05-12 for natural-usage eval. **Gate reset 2026-05-26** — new review gate ≥ 2026-06-09. **FRE-385 ✅ 24h gate passed 2026-05-27** — 3 CL files from 3 real interactions; 2-week promotion gate still running (~2026-06-09). **FRE-387 ✅ 2026-05-26 (PR #82)** — eval session side-effect isolation shipped. FRE-226 needs FRE-227. |
| **G** | Cleanups & gates | Partial | [FRE-265](https://linear.app/frenchforest/issue/FRE-265) ✅ · [FRE-299](https://linear.app/frenchforest/issue/FRE-299) ✅ · [FRE-314](https://linear.app/frenchforest/issue/FRE-314) · [FRE-337](https://linear.app/frenchforest/issue/FRE-337) ✅ · FRE-311 | FRE-265 shipped 2026-05-12; FRE-337 shipped 2026-05-13 (PR #48); FRE-299 gate flipped 2026-05-13; FRE-311 parked on FRE-302 |
| **H** | Memory / context value | Partial | [FRE-375](https://linear.app/frenchforest/issue/FRE-375) ✅ → [FRE-374](https://linear.app/frenchforest/issue/FRE-374) ✅ → [FRE-376](https://linear.app/frenchforest/issue/FRE-376) Phases 1+2+3+4a+4b+5 ✅ (FRE-376 In Progress, Done conditional on 7-day gate) → [FRE-377](https://linear.app/frenchforest/issue/FRE-377) (Approved, Sonnet) → [FRE-381](https://linear.app/frenchforest/issue/FRE-381) (Needs Approval, post-gate) → [FRE-178](https://linear.app/frenchforest/issue/FRE-178) → [FRE-179](https://linear.app/frenchforest/issue/FRE-179) → [FRE-180](https://linear.app/frenchforest/issue/FRE-180) · [FRE-230](https://linear.app/frenchforest/issue/FRE-230) | FRE-375 ✅ 2026-05-22. FRE-374 ✅ 2026-05-22. FRE-376 Phases 1+4a+4b ✅ 2026-05-22. Phase 2 ✅ 2026-05-22 (PR #72). Phase 3 ✅ 2026-05-23 (PR #73). **Phase 5 ✅ 2026-05-23 (PR #74, `cd82179`)**. **FRE-380 Stage 1 ✅ 2026-05-24 (PR #78)**. **FRE-378 ✅ + FRE-379 ✅ 2026-05-24 (PRs #79, #80)** — codex P1 findings closed. FRE-377 Approved; FRE-381 (Stage 2 consolidator decouple) Needs Approval. 7-day gate review ≥ 2026-05-30. Replay still deferred pending gate close. |
| **I** | Prompt observability + user feedback | Planning | [FRE-267](https://linear.app/frenchforest/issue/FRE-267) ~~Canceled~~ · [FRE-403](https://linear.app/frenchforest/issue/FRE-403) EPIC (Needs Approval) · FRE-404 P0 · FRE-405 P1 · FRE-406 P2 · FRE-407 P3 · FRE-408 P4 · FRE-409 P5 · [FRE-183](https://linear.app/frenchforest/issue/FRE-183) · [FRE-184](https://linear.app/frenchforest/issue/FRE-184) | **ADR-0078 + PROMPT_MANAGEMENT_SPEC.md filed 2026-05-28.** FRE-267 (per-session thumbs) canceled — superseded by FRE-407 (P3: per-turn 0–3 rating with `PromptIdentity`). Epic stays In Progress until P5 ships. |
| **J** ✅ | Eval methodology hardening | Done | FRE-329–335 all shipped | See archive for Wave J details |

---

## Pending Verification (merged, not yet tested in production)

*(nothing pending)*

---

## Immediately Actionable (approved, no gate)

| Ticket | Priority | Tier | What |
|--------|----------|------|------|
| [FRE-380](https://linear.app/frenchforest/issue/FRE-380) ✅ | High | Sonnet | Stage 1 — consolidator caps extraction retries + writes stub Turn. Shipped 2026-05-24 (PR #78). 1-week soak feeds Stage 2 design. |
| [FRE-373](https://linear.app/frenchforest/issue/FRE-373) ✅ | High | Sonnet | Skill index mode already flipped to `model_decided`; PR #77 fixed the trace_ctx regression that was breaking the routing call. Closed 2026-05-24. |
| [FRE-372](https://linear.app/frenchforest/issue/FRE-372) ✅ | High | (agent-filed) | Embeddings/pgvector corruption not reproducible today — live `notes_write_executor` end-to-end succeeds with clean 1024-dim vector. Transient incident from 2026-05-16. Closed 2026-05-24. |
| [FRE-378](https://linear.app/frenchforest/issue/FRE-378) ✅ | Medium | Sonnet | /approval session-id mismatch fixed. Shipped 2026-05-24 (PR #79). |
| [FRE-379](https://linear.app/frenchforest/issue/FRE-379) ✅ | Medium | Sonnet | /api/v1/knowledge/* user scoping shipped 2026-05-24 (PR #80). FRE-229 visibility filter now reaches the gateway endpoints. |
| [FRE-375](https://linear.app/frenchforest/issue/FRE-375) | **Urgent** | Opus | Isolate test/eval scripts from production memory + log substrate. 87% of last-7d Neo4j churn is eval-harness pollution. **Blocks FRE-374.** |
| [FRE-374](https://linear.app/frenchforest/issue/FRE-374) | **Urgent** | Opus | Cross-fact constraint layer for memory pipeline (description provenance, relationship consolidation, render fallback, replay from Postgres). **Blocked by FRE-375.** |
| [FRE-369](https://linear.app/frenchforest/issue/FRE-369) | Medium | Sonnet | User-upload UX in PWA with presigned PUT to R2 (multimodal images/files in chat). Spec: ADR-0069 + ADR-0070. **Hold until after 2026-05-30 joinability gate closes** — adds new substrate writes that could complicate the gate. Run sequence: FRE-373 → FRE-372 → FRE-377 → May-30 gate → FRE-374 replay → FRE-369. |
| [FRE-314](https://linear.app/frenchforest/issue/FRE-314) | Medium | Sonnet | `feedback_history/` retention policy in DataLifecycleManager |
| [FRE-349](https://linear.app/frenchforest/issue/FRE-349) | Medium | Opus | Surface actionable Insights in agent context (G3 from FRE-346) |
| [FRE-389](https://linear.app/frenchforest/issue/FRE-389) | High | Opus | **ADR-0076**: Adaptive Constraint Governance Protocol. 3 UI surfaces (Send→Stop, TurnStatusBar, DecisionCard), constraint pause round-trips, user preferences. ADR Codex-reviewed (3 passes, all blockers resolved). **Depends on FRE-388 ✅, FRE-392 ✅. Ready to implement.** |
| [FRE-391](https://linear.app/frenchforest/issue/FRE-391) | Medium | Opus | Dynamic `max_tokens` based on tool/task context — addresses root cause of artifact truncation loop. Infrastructure exists in `budget.yaml` `default_output_tokens`. |
| [FRE-392](https://linear.app/frenchforest/issue/FRE-392) ✅ | Medium | Sonnet | WS transport duplicate message guard shipped 2026-05-27 (PR #85). |

**Calendar-gated (approved but not yet startable):**
- **FRE-350** (Opus) — post-deploy reflection-surfacing eval. **Gate opened 2026-05-24** — now startable. See Immediately Actionable.
- **FRE-328** — naming-stability data review. **Gate reset 2026-05-26** — taxonomy switched to agent-nouns (`{domain}-{noun}`); prior dataset (186 events) was eval-polluted. New gate opens 2026-06-09 (2 weeks of clean production data with updated prompt).
- **FRE-376** (Opus) — close to Done after 7-day joinability green gate ≥ **2026-05-30**; `make joinability-status` exit 0 = ADR-0074 Proposed → Accepted. Reminder routine `trig_01BxH2rKLWAKZ5E9WAS4HcUg` fires 2026-05-30 09:00 UTC and posts the checklist as a Linear comment on FRE-376.
- **FRE-374 purge → replay** — executes **immediately after** FRE-376 closes (same session). Writing to the verified-joinable substrate; D4 (provenance array) decision based on post-replay perf snapshot.
- **FRE-381** — Stage 2 consolidator decoupling. Lands in the same week as FRE-374 replay (both touch consolidator + memory/service code). Requires ADR-0074 §I5 amendment + TurnNode schema change + post-FRE-380 soak data on stub-Turn-attach ratio.

---

## Needs Approval

| Ticket | Priority | Tier | What |
|--------|----------|------|------|
| [FRE-381](https://linear.app/frenchforest/issue/FRE-381) | Medium | Sonnet | **Stage 2** of consolidator decoupling — fully invert Turn-creation vs entity-extraction dependency. Replaces `turn_exists()` with `turn_lacks_entities()` predicate. Adds `extractor_model` to `TurnNode` (schema change). Requires ADR-0074 §I5 amendment for staged `extractor_model`. Codex returned REQUEST CHANGES with detailed adjustments; ticket carries them. **Blocked-by FRE-380 ✅ + May-30 gate.** |
| [FRE-390](https://linear.app/frenchforest/issue/FRE-390) | Low | Sonnet | Eval harness skips transport layer — no automated coverage for WS delivery. End-to-end transport eval needed. |

---

## Key Dependencies

```
FRE-213 ✅ → FRE-342/343/344 (unblocked) → FRE-227 (paused) → FRE-226 (self-updating skills)
FRE-178 → FRE-179 → FRE-180  (recall L2/L3/gap chain)
FRE-214 ✅ → FRE-238/240/241/236 + FRE-336 + FRE-338–341 (unblocked, deferred §8.7)
FRE-263 ✅ → FRE-265 ✅ (delete, 2026-05-12)
FRE-325 ✅ → FRE-326 ✅ (consolidation re-eval, retained as deployment-conditional)
FRE-348 ✅ → FRE-350 (eval) gate ≥ 2026-05-24
FRE-328 capture pipeline ✅ → FRE-328 naming-stability decision gate reset 2026-05-26 → new gate ≥ 2026-06-09 (agent-noun taxonomy deployed)
FRE-346 ✅ → FRE-347 ✅ → FRE-348 ✅ → FRE-349 (G3, unblocked)
ADR-0068 ✅ (FRE-351/352/353/354/355/356 all done)
FRE-302 ✅ → FRE-311 (budget auto-tuning, parked pending data)
FRE-375 (test/prod isolation) → FRE-374 (memory integration ADR + replay)
FRE-388 (WS transport, ADR-0075) → FRE-392 (WS dedup, concurrent) + FRE-389 (constraint governance, ADR-0076)
FRE-391 (dynamic max_tokens) — independent of FRE-389 but addresses root cause of the motivating example
```

---

## Recently Completed

| Item | Date | Summary |
|------|------|---------|
| **FRE-410 follow-up: directive grep-first read marker (PR #100)** | 2026-05-28 | Kept the 200-line/8 KB default but rewrote `_build_read_marker` + the `read` tool description into plain, imperative grep-first guidance — FIND (`grep -n`) → READ (`offset`/`limit`) → REPEAT → answer; states the unseen-line count; embeds the resolved path so the grep example is copy-pasteable. Distinct `line_clipped` marker points to a larger `max_bytes` for an oversized single line. 22 tests pass; mypy + ruff + pre-commit clean. Deployed + verified live: the local SLM grep'd the FSM symbols then paged `executor.py` offset=1095 + `loop_gate.py` offset=195 to produce a correct, complete two-FSM answer from targeted reads — not the full-file slurp. |
| **FRE-410: `read` primitive head cap + offset/limit ranged reads (PR #99)** | 2026-05-28 | The cheap half of FRE-401. `read.py` defaults to a truncated head (first `DEFAULT_LINE_LIMIT`=200 lines / `DEFAULT_HEAD_BYTES`=8 KB) with `truncated=true` + a continuation `marker`; adds 1-based `offset`/`limit` paging (`limit` clamped ≥1). New `_read_line_window` enforces the byte cap on whole-line boundaries so paging never loses/duplicates content — the continuation offset re-reads any excluded line; a single oversized line is clipped with a distinct marker pointing to a larger `max_bytes`. `too_large` error removed (registry + tests were the only callers); `tail_lines` keeps `LEGACY_TAIL_CAP`=1 MiB (no log regression); `session_id` threaded alongside `trace_id` + new `offset`/`limit`/`lines_returned`/`total_lines` log fields (ADR-0074). 22 module tests (+empty file, head no-trailing-newline, two-call paging across byte cap); 2765 pass; mypy + ruff + pre-commit clean. Codex-reviewed twice (plan NEEDS REWORK→fixed; diff found 2 boundary bugs→fixed→APPROVE). Live: `executor.py` 133,969 B (~33K tok) → 7,204 B (~1.8K tok, 200/3,335 lines) — ~31K-token single-read reduction (AC#5). |
| **FRE-392: WS duplicate message idempotency guard (PR #85)** | 2026-05-27 | ADR-0075 follow-up. `MessageDeduplicator` in `service/idempotency.py` — in-process dict keyed by `(session_id, client_msg_id)` or SHA-256 content hash, 120s TTL, `release()` on task completion. `POST /chat/stream` returns `deduplicated: true` for duplicate POSTs; `POST /chat` raises 409. `trace_id` moved to endpoint level and threaded through `_process_chat_stream_background`. 60s lifespan cleanup task. PWA: `clientMsgId` (UUID v4) generated per `sendMessage` call and passed as `client_msg_id` form field. 15 unit tests; 2664 pass; mypy + ruff clean. |
| **FRE-388: WebSocket transport + durable channel (PR #83 + 8 hotfixes)** | 2026-05-27 | ADR-0075. SSE→WS migration, verified live on iPad. Backend: Postgres `session_events` table (migration 0005) with global seq for reconnect replay; `ws_endpoint.py` (full WS lifecycle: ticket auth, multi-connection eviction 4001, sender/receiver tasks with single-reader discipline, per-connection decision waiter registry, PING/PONG, rate limiting); `event_buffer.py` (append, replay, REPLAY_GAP, TTL cleanup); `ws_ticket.py` (single-use 30s tickets for browser WS auth); `transport.py` rewritten for dual-write (Postgres seq assigned before enqueue for dedup); `adapter.py` gains `seq` on all envelopes; `POST /api/ws-ticket` endpoint; hourly cleanup task. Removed: `endpoint.py` (SSE), `approval_waiter.py` (Future registry). PWA: native WebSocket client with ticket auth, parallel-connection guard, exponential backoff reconnect (1s..30s + jitter), 25s PING heartbeat, `last_seq` localStorage persistence, client-side seq dedup in hook, REPLAY_GAP fallback to REST API; approval/interrupt decisions via WS; removed `@microsoft/fetch-event-source`. Post-merge hotfixes (8): TS strict-mode cast; `CAST()` vs `::jsonb` for asyncpg; Caddy `/ws/*` routing (snippet path matcher bug — required dedicated handle block in agent site); CORS `allow_origins` for WS upgrades; `allowed_ws_origins` missing production domain; WS-before-sendMessage ordering; DONE event persistence; concurrent WS read conflict (Codex); server+client seq-based dedup. Config: `claude_sonnet.max_tokens` 8192→32768, `default_timeout` 60→180. 2630 tests pass, mypy clean, zero gateway errors in production. FRE-390 filed (eval transport coverage gap, Low). FRE-391 filed (dynamic max_tokens per tool/task, Medium). |
| **FRE-387: eval session side-effect isolation (PR #82)** | 2026-05-26 | `if not ctx.eval_mode:` wraps the capture + `RequestCapturedEvent` + reflection block in `execute_task` — one gate blocks the entire downstream pipeline (consolidation → entity extraction → Neo4j → insights → promotion). Defense-in-depth: `_trigger_captains_log_reflection` returns immediately on `eval_mode=True`. `RequestCompletedEvent` gains `eval_mode: bool = False`; `build_request_trace_es_handler` skips ES trace indexing for eval events. Session writer NOT gated (multi-turn eval needs DB writes). 7 new tests; 2637 pass; mypy + ruff clean. |
| **FRE-376 Phase 3: identity threading + back-compat removal (PR #73, `6f63f55` + fix `af012f6`)** | 2026-05-23 | ADR-0074 §I3/§I5 / §I4. **Audit lint** (`scripts/check_identity_threaded.py`, 244 lines, AST-walks `log.*` / `bus.publish` / Cypher `MERGE`) becomes the definition-of-done — wired into pre-commit; exits 0 on the whole `src/personal_agent/`. **bus.publish**: all 21 sites verified via lint upgrade to recognize typed Pydantic Event payloads (`RequestCapturedEvent`, `MemoryAccessedEvent`, `MetricsSampledEvent`, etc.) where the model already enforces `trace_id`/`session_id` at construction; no source changed there. **Cypher MERGE / §I5**: `:Turn` and `:Entity` writes now set `originating_trace_id` / `originating_session_id` (+ `extractor_model` on entities from `qwen3-8b`); `MemoryService.create_entity()` signature extended with the three origination kwargs; consolidator + gateway-store_fact callers plumbed through. **executor.py:1640 collision resolved**: orchestrator step boundary renamed to `STEP_PLANNING_STARTED` / `STEP_PLANNING_COMPLETED` (emitted on success, tool-execution branch, AND error path before `return TaskState.FAILED`) — `MODEL_CALL_*` is exclusively client-side from here on. **Phase 2 back-compat aliases removed atomically with consumers**: `emit_legacy_litellm_start/complete` helpers deleted; `model_id`/`prompt_tokens`/`completion_tokens`/`tokens`/`cache_write_tokens` stripped from canonical emit payloads; structlog kwargs in `client.py`/`dspy_adapter.py`/`entity_extraction.py`/`session_summary.py` renamed to canonical; `docker/elasticsearch/index-template.json` mappings + `config/kibana/setup_dashboards.py` generator + 4 regenerated NDJSON dashboards (`llm_performance`, `request_traces`, `request_timing`, `data_views`) + `tests/evaluation/run_primitive_tools_eval.py` all re-pointed to `model_call_completed` + `input_tokens` / `output_tokens` in one PR. **200+ structlog kwargs threaded** across orchestrator, memory (33 sites + 11 method signatures extended), captains_log (29 + helper `_trace_id_from_entry`), mcp (9 + ctx-injection convention), gateway/service (22 + `SystemTraceContext.new(<source>)` for non-handler paths), telemetry/cost_gate/config (17 + `CostGate.commit/refund` kwargs), brainstem/second_brain (71 + new minting helpers `_new_scheduler_trace_id`/`_new_consolidation_trace_id`/`_new_qm_trace_id` per §I4 — consolidator now mints `system:consolidation` rather than the legacy `"consolidation"` literal), events/llm_client/tools/storage/security (44 + `R2ArtifactStore.put/get/delete` kwargs, sandbox/`_check_path_governance` kwargs). **~370 allowlist entries** for genuinely context-free sites — each with a per-function reason (sensor hardware polls, FastAPI lifespan, MCP gateway init, AG-UI SSE handshake, anyio cancel-scope cleanup, protocol-contract boundaries that lack ctx without a wider refactor). Tests: 2549 pass (added `test_neo4j_origination_properties.py`, `test_step_planning_events.py`, `test_bus_publish_carries_identity.py`, updated `test_telemetry_parity.py` + `test_litellm_emit_payload.py` + 2 test mock files). mypy 228 files clean; ruff clean. **Deferred to follow-up FRE**: `captains_log/capture.py:61` `CaptureEntry` dataclass schema canonicalization (replay-affecting); tightening Phase 3 `trace_id: str \| None = None` kwargs to non-optional `TraceContext` per Phase 4 (I6) when Phase 5 ships and confirms steady state. Plan: `docs/superpowers/plans/plan-fre-376-phase-3-magical-adleman.md`. |
| **FRE-376 Phase 2: equalize model client telemetry (PR #72)** | 2026-05-22 | ADR-0074 §I2. Both model clients now route through a shared `src/personal_agent/llm_client/telemetry.py` helper module that owns the canonical event shape: `emit_model_call_started` / `emit_model_call_completed` produce `(model, role, endpoint, trace_id, session_id, span_id, parent_span_id, latency_ms, input_tokens, output_tokens, total_tokens, cache_read_tokens)`. `CANONICAL_MODEL_CALL_STARTED_FIELDS` and `CANONICAL_MODEL_CALL_COMPLETED_FIELDS` frozensets exported from `telemetry/events.py` are the single source of truth — the parity test imports them as the contract, so adding a required field forces every model client to populate it. Back-compat: `litellm_request_start` / `litellm_request_complete` co-emit alongside canonical events; `model_id` / `prompt_tokens` / `completion_tokens` field aliases double-written for one release cycle (Phase 3 cleanup removes). `LiteLLMClient` now mints a span via `trace_ctx.new_span()` so started/completed share `span_id` (the started event previously had no `span_id` at all). **Tests** (`tests/personal_agent/llm_client/test_telemetry_parity.py`): 6 tests across 3 layers — `TestCanonicalEmitContract` (helpers cover the frozensets), `TestClientWiring` (each client invokes the helpers with right `trace_ctx`/`span_id`; LocalLLMClient short-circuits via `httpx.TimeoutException` to avoid faking the streaming response), `TestLegacyBackCompat` (deprecated names still emit). 2554 unit pass / 50 skipped; mypy 228 files clean; ruff clean. **Post-deploy on cloud-sim VPS**: drove one cloud turn (`profile=cloud`) and one local turn (`profile=local`); ES `agent-logs-2026.05.22` contains `model_call_completed` from both `endpoint=anthropic` (model `anthropic/claude-sonnet-4-6`, input=8465 / output=4 / latency=1377ms) and `endpoint=https://slm.frenchforet.com/v1/chat/completions` (model `unsloth/qwen3.6-35-A3B`, input=7592 / output=170 / latency=10116ms) — both rows carry `session_id`, `span_id`, `parent_span_id`. AC-8 verified: `litellm_request_complete` still emitting with legacy `tokens`/`prompt_tokens`/`completion_tokens` fields (36 hits today). **Observation flagged for Phase 3**: orchestrator at `executor.py:1640` also emits `MODEL_CALL_STARTED` with only `trace_id`/`span_id`/`model_role`/`channel` — pre-existing two-layer event-name collision; Phase 3 will reconcile (either rename to `step_planning_started` or thread the full canonical tuple). Plan: `docs/superpowers/plans/ancient-fluttering-ullman.md`. |
| **FRE-376 Phase 1: end-to-end traceability — cost identity + per-message attribution** | 2026-05-22 | First slice of ADR-0074. **Schema** (migration `0004_traceability_identity.sql` + `init.sql` mirror): purged 4,077 NULL-trace `api_costs` rows (unattributable per ADR), flipped `api_costs.trace_id` to `NOT NULL`, added `api_costs.session_id` (NULL-able pending Phase 2 confirmation that the local-LLM path also threads identity), added `sessions.primary_model_at_creation` + `sessions.model_config_path`. **Write-time enforcement**: `CostTracker.record_api_call` now requires `trace_id: UUID` + `session_id: UUID` and raises `MissingIdentityError` (new `personal_agent.exceptions` module — referenced by CLAUDE.md but never previously created). `SessionRepository.append_message` raises `InvalidMessageError` when an assistant message lacks `model` / `model_role` / `model_config_path` (top-level OR under `metadata`). **Plumbing**: `LiteLLMClient.completion` threads identity + `budget_role` into `record_api_call`; when `trace_ctx.session_id` is absent (background pipelines, scheduler ticks — Phase 4 work) it logs `cost_record_missing_identity` and skips rather than blowing up the request. `service.app` (3 session-creation sites + 2 in-process assistant-message appends) and the Redis `cg:session-writer` consumer (`events/request_completed_handlers.build_session_writer_handler`) both resolve attribution via the shared `config.model_loader.resolve_active_attribution()` so on-process and out-of-process append paths produce identical metadata. **Tests**: 3 new unit tests for the cost-tracker contract, 7 for the assistant-message validation (covering metadata vs. top-level placement, partial fields, and that user/tool/system messages bypass), 1 migration test that exercises 0004 against an ephemeral schema (skips when the FRE-375 test stack is unreachable). 2542 unit tests pass / 50 skipped (no regressions); `make mypy` clean (227 files); `make ruff-check`/`format` clean. **Post-deploy verification on VPS prod-sim**: applied migration (`DELETE 4077`), rebuilt gateway, ran a real chat turn — AC-5 ✅ three `api_costs` rows for the trace carry both `trace_id` and `session_id`; AC-6 ✅ new sessions populate both attribution columns; AC-7 ✅ assistant message records `model` + `model_role` + `model_config_path` in its `metadata`. **Known imperfection** (deferred to Phase 2): `model` in assistant metadata records the *configured-primary* (qwen3.6-35-A3B) rather than the *actual call model* (sonnet-4-6 when the cloud profile is active); cost rows in `api_costs` carry the real model, so cost attribution is precise even when the message-level attribution is approximate. **Phases 2-5 remain**: `LocalLLMClient` event-shape equalization, audit every emit site, `TraceContext` non-optional + `SystemTraceContext`, pre-commit lint + AST audit + joinability probe (7-day green requirement). ADR-0074 stays **Proposed** until all phases ship. Plan: `docs/superpowers/plans/implement-fre-376-check-master-encapsulated-umbrella.md`. |
| **FRE-368: agent artifact tools + PWA inline cards (PRs #66 + #67)** | 2026-05-21 | Full ticket shipped in two PRs. **Backend (PR #66)**: `artifact_write`, `artifact_list`, `artifact_read` tools (R2ArtifactStore + Postgres, 5 MB cap, 6-type allowlist, pgvector embeddings); public `GET /api/v1/artifacts` + `GET /api/v1/artifacts/{id}` CF-Access-gated endpoints; `POST /api/v1/telemetry/card_click` for ADR-0070 D8 measurement; governance entries; 43 unit tests. **PWA (PR #67)**: `ArtifactCard` inline Tier 3 cards in chat (URL interception in MarkdownContent.tsx); `ArtifactViewer` sandboxed iframe (right drawer desktop / bottom sheet mobile, `sandbox=""`); `/artifacts` compact list route with back nav; `agui-client.ts` helpers; SW cache bump. **Two production bugs found and fixed on branch before merge**: SQL NULL type inference (`CAST(:prefix AS TEXT) IS NULL`); auth AUD mismatch on public endpoints (agent app JWT ≠ artifacts AUD — switched to `get_request_user`). All ACs (AC-4 through AC-10) verified live on iPad before merge. ADR-0070 D8 two-week review gate ≥ 2026-06-04. |
| **FRE-368 backend: artifact_write/list/read tools + public endpoints (PR #66)** | 2026-05-21 | `artifact_write`, `artifact_list`, `artifact_read` tools live (R2ArtifactStore writes, Postgres rows, pgvector embeddings, 5 MB cap, 6-type content_type allowlist, base64 for PNG). Public `GET /api/v1/artifacts` + `GET /api/v1/artifacts/{id}` CF-Access-gated endpoints for the PWA. `POST /api/v1/telemetry/card_click` for ADR-0070 D8 measurement. 43 new tests (27 tools, 8 public endpoints, 8 telemetry). `artifact_tools_registered` confirmed in startup logs. **AC-5 + AC-6 still pending** — require a real PWA session to verify write→list→read round-trip and Worker serving `type='artifact'` rows on iPad. PR #B (PWA inline cards + `/artifacts` route) is next. |
| **FRE-227 + FRE-371: R2-backed artifact substrate shipped end-to-end (PRs #62, #63, #64, #65)** | 2026-05-17 | Substrate trio landed across two repos in one day. **Public repo (`personal_agent`)** — PR #62 (the substrate proper): Postgres `artifacts` table + migration 0003 + `init.sql` mirror, `R2ArtifactStore` async aiobotocore wrapper with strict prefix-escape guard at `build_r2_key`, `notes_write` + `notes_search` tools (revision-per-write append, pgvector-NN search), `/internal/artifacts/{id}` resolve endpoint, governance entries, 45 unit tests. PR #63 — `fix: thread user_id/session_id through TraceContext to tool executors`: latent bug since FRE-343 (2026-05-14) where the orchestrator stripped `user_id` when constructing the per-task `TraceContext`, breaking any tool that needs per-user scoping (notes_*, recall_personal_history). 5 regression tests including a source-level guard. PR #64 — `fix: serialize pgvector as text literal before asyncpg binding`: asyncpg has no built-in pgvector codec, so `CAST($n AS vector)` with a `list[float]` fails; helper `_pgvector_literal` renders `[v1,v2,...]` text form for both INSERT and ORDER BY paths. PR #65 — `fix(security): require CF Access JWT verification on artifact-resolve endpoint`: cryptographically-verified `Cf-Access-Jwt-Assertion` now gates `/internal/artifacts/{id}`; plaintext `X-Authenticated-User-Email` is explicitly ignored. New `service/cf_access_jwt.py` with cached JWKS, RS256 + aud + exp + email claim enforcement, 23 tests. **Private repo (`personal_agent_secrets`)** — R2 bucket (`seshat-artifacts`, EEUR location — Dev-1: EU jurisdiction dropped because CF Workers binding API error 10085 can't locate jurisdiction-namespaced buckets); Workers Paid Worker `artifacts-substrate` with R2 binding + `crypto.subtle`-based JWT verification; **dedicated** `cloudflare_zero_trust_access_application.artifacts` (Dev-2 reverted same-day after H1 finding: CF Access `destinations` consolidation only injects `Cf-Access-Jwt-Assertion` on the primary `domain`, not secondary entries — the substrate's identity chain collapsed silently for ~30 minutes until LC re-created the dedicated app with new AUD `9cd9a21bc4773…`); workers.dev URL killed at the dashboard level. **ADRs amended** — ADR-0069 records Dev-1/2/3 deviations and the auth hardening; ADR-0064 records the partial JWT verification implementation (artifacts endpoint only; SSE / chat / sessions still header-trust as a Wave E follow-up). **Verified end-to-end** at 08:08 + 08:11 UTC: fresh `notes_write` → 200 OK from gateway → markdown bytes render in iPad Safari via the Worker. **Forensic note**: a spurious `users` row for `starry-plaza-1s@icloud.com` (LC's Linear MCP `createdBy` email, leaked via diagnostic Worker test) materialized at 04:45:18 UTC during pre-hardening testing; deleted as part of close-out. **One UX gap remains** (handed to FRE-368): the agent PWA opens artifact URLs in WKWebView, which doesn't share cookies with Safari proper — fix is `target="_blank"` on the public_url anchor, lives naturally in FRE-368's existing PWA scope. |
| **FRE-365: Compressor model upgrade + system prompt expansion (PR #61)** | 2026-05-16 | Empirical diagnosis falsified the ticket's framing on three fronts: (a) cache plumbing already runs for all Anthropic models at `litellm_client.py:232` (not Sonnet-only), (b) OpenAI's `cached_tokens` is already extracted at `litellm_client.py:352-360`, (c) the proactive context-budget gate the ticket asked for already exists at `executor.py:1377` (`needs_hard_compression` at 0.85 × `context_window_max_tokens`). The ticket's headline numbers (4.5M tokens/day, 118K-token calls) did not match ES (~2.3M total, max 34 643). Genuine findings: gpt-5.4-nano does NOT participate in OpenAI automatic prompt caching (verified via 1640-token stable-prefix A/B — nano returns cached_tokens=0 across all calls, mini returns 1280 cached from call 2 onward); Haiku 4.5 production prompts (avg 652, max 834 tok) sit below Anthropic's cache floor by design; the compressor system prompt was structurally uncacheable at ~188 tokens. PR ships: (1) `_COMPRESSOR_SYSTEM_PROMPT` expanded to ~1065 tokens (tiktoken `cl100k_base`) via three worked examples + anti-patterns section, locked by tiktoken-threshold test; (2) `config/models.cloud.yaml` background-role rebalance — `compressor` & `entity_extraction_role` nano→**gpt-5.4-mini**, `captains_log_role` & `insights_role` nano→**claude_sonnet** (historical commented-out config restored). Background-pipeline cost rises from ~$1.13/mo to ~$7.68/mo at observed volumes — sub-dollar absolute delta on roles that feed the agent self-improvement loop. Empirical caveat: at 1065 tokens of stable prefix, mini did not engage cache in the harness (only at ~1500 did); OpenAI's effective cache floor for mini is higher than the documented 1024 line. Gateway rebuilt and verified: config baked into image shows new assignments, factory resolves `compressor → openai/gpt-5.4-mini`, live compressor invocation from inside the container succeeded with the new prompt. Diagnostic doc: `docs/superpowers/plans/2026-05-16-fre-365-cache-diagnostic.md`. |
| **FRE-366: SearXNG weather-engine crash fix + X-Forwarded-For (PR #60)** | 2026-05-16 | `src/personal_agent/tools/web.py` now strips a leading `weather ` token from queries when targeting weather (`categories="weather"` or `engines` mentions `wttr.in` / `openmeteo` / `duckduckgo_weather`). Before fix, raw `weather Berlin` made wttr.py and duckduckgo_weather feed the full string into a geolocation lookup and crash with `ValueError: unknown geo location: 'weather Berlin'`, leaving both engines unresponsive on every weather-targeted query. General-category searches keep the literal query. Also sends `X-Forwarded-For: 127.0.0.1` on the outbound httpx GET so SearXNG's botdetection stops logging `ERROR: X-Forwarded-For nor X-Real-IP header is set!` on every internal call (limiter itself is already off in `docker/searxng/settings.yml` — this is purely log hygiene). 6 new unit tests (20/20 pass), mypy clean. Live verified against `cloud-sim-searxng`: before fix `unresponsive_engines=[["duckduckgo weather","unexpected crash"],["wttr.in","unexpected crash"]]`; after fix wttr/openmeteo return 1 weather answer per query with zero botdetection or geo-location errors. Original Linear ticket framing ("worker crashed, port 8080 not bound, full outage") was falsified during analysis and downgraded Urgent → Medium before implementation. |
| **FRE-370: Cloudflare Access `session_duration` → 720h** | 2026-05-15 | Operational fix landed from local-machine terraform apply against `personal_agent_secrets/infrastructure/terraform-cloudflare/`. Six resources updated: `policy.personal_only` (24h→720h), `policy.backend_admin` (1h→720h), `app.app_launcher` (24h→720h), `app.agent` (24h→720h + `auto_redirect_to_identity=true`), `app.monitoring` (1h→720h + auto-redirect), `app.graph` (1h→720h + auto-redirect). Eliminates daily re-auth pain across all CF Access-gated apps (slm, PWA, monitoring, graph). 30-day per-device sessions; re-auth still triggers on new device, explicit logout, token revocation, or IdP-side invalidation. Commit `800c249` in `personal_agent_secrets`; zero changes in public repo. Tier-3:Haiku. |
| **ADR-0069 + ADR-0070: Artifact substrate + output channel model (specs)** | 2026-05-15 | Two ADRs landed in Proposed status documenting the architecture for agent-produced and user-uploaded artifacts. **ADR-0069** (R2-Backed Artifact Substrate): Cloudflare R2 (EU jurisdiction) + Worker on Workers Paid + Postgres `artifacts` table (with pgvector) as metadata canon + native `aiobotocore` S3 SDK on the agent side (no FUSE mount). Flat content-addressable public URLs (`artifacts.frenchforet.com/{artifact_id}`); hierarchical R2 keys (`{type}/{user_id}/{session_id}/{artifact_id}_{slug}.{ext}`). Single bucket, three flows (agent-write / user-upload / URL-ingest). Identity and authorization inherited from ADR-0064; terraform in private secrets repo. **ADR-0070** (Output Channel Model — Markdown for Agents, Rich for Humans): three channels with consumer-determined format. Agent↔agent → markdown (memory, sub-agents, MCP, captain's log). Agent→human-conversational → hybrid markdown + Mermaid + artifact cards. Agent→human-persistent → HTML in R2. Single-surface chat with progressive disclosure (inline expand / drawer / standalone). Sandbox posture "documents not apps" by default. Dual-representation refactor (`reply_html` alongside `reply_markdown`) explicitly deferred until FRE-368 measurement (D8) justifies it. Status: both ADRs Proposed pending owner acceptance; implementation tickets FRE-227 / FRE-368 / FRE-369 Approved. Companion discussion record: `docs/superpowers/plans/i-want-to-research-bubbly-shannon.md`. |
| **FRE-343: Personal time-window retrieval (PR #57)** | 2026-05-14 | Adds `(:Person)-[:PARTICIPATED_IN]->(:Turn)` provenance edge (W3C-PROV per ADR-0052 §Update 2026-05-14). New native tool `recall_personal_history(days_ago, topic?, limit?)` scoped to `ctx.user_id`; complementary SKILL.md `personal-history-recall.md` with semantic-XML body pilot (`<when_to_use>`, `<example>`, `<anti_example>`) extending FRE-337 to author-side. `TaskCapture.user_id` tightened to non-optional `UUID` — verified `get_request_user` always resolves one (CF Access or owner-email fallback or 401). One-shot idempotent backfill (`make backfill-participated-in`); pre-FRE-343 capture JSON files get nil-UUID injection at parse time so read paths keep working. Default `search_memory` shared-graph behavior **unchanged** — personal-history is explicit opt-in via self-scoping phrasing ('we', 'I', 'my', 'remind me'). Quality gates clean: 2346 tests, mypy 218 files, ruff clean. |
| **FRE-315: Mermaid rendering + validated authoring skill + iOS field-test follow-ups (PRs #53, #56)** | 2026-05-14 | **Initial shipment (PR #53)** — Two halves together. *PWA half*: `seshat-pwa/src/components/MermaidBlock.tsx` integrates mermaid.js v11 into `MarkdownContent.tsx` — lazy chunk-split via dynamic import (mermaid never appears in the initial bundle, confirmed across 5 lazy chunks), custom `theme: 'base'` + `themeVariables` matching the seshat slate/blue-500 palette, figure/source toggle with 150ms cross-fade, graceful syntax-highlighted fallback with a rose-tinted error annotation on parse failure. SVG inserted via `DOMParser` + `replaceChildren` under `securityLevel: 'strict'` — no string-injection HTML patterns. *Skill half*: `docs/skills/mermaid-diagrams.md` with full frontmatter (44 keywords, nudge directive per ADR-0067), 9 syntax invariants for v11, six worked examples (flowchart, sequence, state, class, C4, ER), and an **automated validation pattern** using `mmdc` in-container before the diagram reaches the user. `Dockerfile.gateway` gains `nodejs`, `chromium`, `fonts-liberation`, `@mermaid-js/mermaid-cli@^11` with an `/usr/local/bin/mmdc` wrapper that injects `--no-sandbox` via puppeteer config (Chromium-as-root requirement); `mmdc` added to bash auto-approve in `config/governance/tools.yaml`. Image grew ~800MB — accepted cost for closing the LLM-authored-mermaid failure loop. Discovered+documented mmdc quirk: exits 0 even on parse error, so the skill validates by output-file presence (`test -s /tmp/m.svg`) rather than `$?`. Linear FRE-315 description expanded during planning to add the skill scope. **iOS field-test follow-ups (PR #56, stacked on #54/#55)** — Three real-world bugs surfaced during in-PWA testing on iPhone: (1) installed iOS PWA kept serving the pre-merge bundle (cached app shell with static `CACHE_NAME='seshat-v1'`); fix: SW rewritten to **network-first** with cache fallback for GET, network-only bypass for `/stream`/`/chat`/`/api/*`, and `CACHE_NAME` is now the eviction lever bumped on each shell-changing deploy — convention saved to memory as `project_pwa_sw_cache_convention.md`. (2) C4 diagrams failed to render with `Namespace prefix xlink for href on image is not defined` — mermaid's browser-side `render()` emits SVG fragments with `xlink:href` but no root `xmlns:xlink`; fix: switched DOMParser ingestion from strict `image/svg+xml` to `text/html`, which handles SVG-in-HTML per WHATWG and preserves SVG attribute case. (3) No way to copy/save the rendered figure; added three header actions (download SVG, download PNG via canvas rasterization at DPR=2 with seshat-dark background, copy SVG markup), each with a 1.4s checkmark flash on success; labels collapse to icon-only on narrow iPhone viewports. Final `CACHE_NAME = 'seshat-v4-fre-315-image-actions'`. |
| **FRE-326: host-resource gate re-evaluation (no code change)** | 2026-05-14 | Investigated `_should_consolidate()` resource gates (`idle_time_seconds`, `cpu_threshold`, `memory_threshold`, `resource_gating_enabled`) per the post-FRE-325 mandate. ES query over 8-day window (2026-05-06 → 2026-05-14): 405 `consolidation_triggered`, 117 `consolidation_skipped_active_requests`, 19,487 `consolidation_skipped_already_consolidated`, **zero** `consolidation_skipped_cpu_high` / `_memory_high` / `check_failed`. The zero counts are tautological — gates are disabled via `AGENT_SECOND_BRAIN_RESOURCE_GATING_ENABLED=false` (the experimental condition), so they cannot fire. The honest finding: under remote-inference deployment the system operated healthily for ≥8 days with only the universal guards (`_active_request_count`, `min_consolidation_interval_seconds`) active. **Verdict**: retain gates as deployment-conditional safety valve. They serve the local-inference deployment mode (agent + LLM co-resident on Apple-silicon laptop running MLX), where host CPU/memory/idle metrics are a meaningful proxy for inference saturation; under the current remote-inference deployment they're correctly dormant. Artifact: docstring + comment updates on `_should_consolidate`, `brainstem/optimizer.py`, `config/settings.py`, `.env.example`; ADR-0041 §Update 2026-05-14 documenting the verdict. No code change. |
| **FRE-363: operator stanza missing from gateway execution path** | 2026-05-13 | `get_owner_stanza()` was only called in the legacy non-gateway branch, which is unreachable in production. Every production request hit the gateway path which had no stanza call — `## Operator / You are assisting {name}` was never injected. Fix: add stanza population to the gateway branch using the global memory_service. Discovered during FRE-344 follow-up. PR #51. |
| **FRE-344: display_name config-driven seeding (FRE-213 follow-up)** | 2026-05-13 | `AGENT_USER_DISPLAY_NAMES_JSON` env var seeds `users.display_name` (Postgres) and `:Person.name` (Neo4j) idempotently at startup. Coalesce rule: only overwrites NULL or email local-part. All 4 CF Access users seeded (Alex, Susan, Erika, Laurent). PR #50. |
| **FRE-299: Flip graph_quality_governance_enabled gate (ADR-0060 P2-3)** | 2026-05-13 | Set `AGENT_GRAPH_QUALITY_GOVERNANCE_ENABLED=true` in `.env` after 14-day Phase 1 validation (33 anomalies published, 0% false-positive rate). `governance_enabled=True` confirmed in gateway startup logs. |
| **FRE-341: Prune execution-service gateway token** | 2026-05-13 | Deleted stale `execution-service` token block from `config/gateway_access.yaml`. ADR-0045 amended 2026-05-08 confirmed orchestrator and gateway are the same process — the role was never used. Config-only change, no tests needed. Direct-to-main commit. |
| **FRE-337: Skill nudge injection + XML headers (ADR-0067)** | 2026-05-13 | Two deterministic XML directive blocks (`<skill_index_directive>` and `<skill_usage_directives>`) appended after all skill content in the system prompt, immediately before the user message. Closes the behavioral gap where the primary model (recall=1.0 from router) answered from training-data priors instead of executing the injected skill. `nudge:` YAML frontmatter field added to `SkillDoc`; seeded on 4 skills. Feature-gated via `AGENT_SKILL_NUDGE_ENABLED`. ADR-0067 accepted. PR #48. Follow-up PR #49: `SKILL_BLOCK_HEADER` wrapped in `<skill_library>` XML; redundant `read_skill` instruction removed from index header. Eval: Family A 5/5 live tool use (was 0/5), Family B 4/4 knowledge-only (no regression), adversarial guard held. |
| **FRE-328: capability-gap capture → Linear pipeline (parked for eval)** | 2026-05-12 | Full pipeline shipped across PRs #43–47 and verified end-to-end on the live gateway.  Two signal sources feed the same ES bucket and aggregation: (a) `read_skill` emits `missing_skill_requested` when called with an unknown name; (b) DSPy reflection populates `missing_skill_names` after each task, parsed and emitted from the main loop with `source="reflection"`.  `TelemetryQueries.get_missing_skill_buckets` aggregates by `requested_name` with a cardinality sub-agg on `session_id`.  `InsightsEngine.detect_missing_skill_patterns` produces an `Insight` when count ≥3 across ≥2 distinct sessions, which then flows through `create_captain_log_proposals` → `CaptainLogManager.save_entry()` → `PromotionPipeline` → `Needs Approval` Linear ticket.  Field test surfaced a naming-stability problem: the reflection model names the same conceptual gap differently across sessions (e.g. `word-count-log`, `word-count-history`, `word-count-log-update`, `rolling-sum-7-day`), so every bucket sits at count=1 and the threshold cannot fire without name normalization.  **Parked 2026-05-12 to capture data naturally**; review gate 2026-05-26 with candidate normalization approaches documented in `docs/plans/fre-328-evaluation-window.md`.  Three reusable lessons captured there: (1) `asyncio.to_thread` logs don't reach the ES handler; (2) structlog `event` is stored as ES field `event_type`, no `.keyword` sub-field on already-keyword fields; (3) LLM-generated identifiers are not stable across sessions — any pipeline that keys on them for cardinality must normalize. |
| **FRE-265: Delete 8 legacy tool modules (ADR-0063 P6)** | 2026-05-12 | After 14 days of `AGENT_LEGACY_TOOLS_ENABLED=false` untouched in production, removed: 7 tool modules (`filesystem`, `system_health`, `self_telemetry`, `elasticsearch`, `fetch`, `sysdiag`, `infra_health`) covering 8 tool surfaces, 6 obsolete test files, governance entries in `tools.yaml`, the `legacy_tools_enabled` setting + `AGENT_LEGACY_TOOLS_ENABLED` env var (incl. `docker-compose.eval.yml` control profile and `.env.example`), the deprecated `GovernanceContext.allowed_tool_categories` field (FRE-260 stub), and the unused `allowed_categories` param from `ToolRegistry.get_tool_definitions_for_llm`. Stripped dead tool name references from intent-pattern regex, fallback reply text, and deployment-hint scaffolding in `orchestrator/executor.py`. Updated `prompts.py` to point at `bash` + skill docs for fetch/list operations. Tests rewritten (`test_primitives_registration.py`, `test_legacy_tools_deprecation.py` removed). Net −20 ruff and −10 mypy errors. ADR-0063 fully closed; `git revert` is now the rollback path. |
| **FRE-342: Person dedup excludes user_id-bound :Person** | 2026-05-12 | `memory/dedup._find_similar_entities` Cypher gains `AND node.user_id IS NULL` so harness owner/user-anchored `:Person` nodes (FRE-213 schema, ADR-0052 amendment) are never returned as merge candidates. Prevents extracted third-party "Alex" from colliding into `:Person {user_id, is_owner:true}` and destroying the anchor. Unit test asserts filter present in issued Cypher. PR #41. |
| **Post-PR-#34 eval stabilization (PRs #35–40)** | 2026-05-11 | Six PRs in one day. (1) Harness alignment for PR #34 changes: ES `trace_id.keyword`→pure keyword, `--cf-email` flag for cloud profile auth, retry config for 5s ES refresh — PR #35. (2) Config aligned with Qwen3.6-35B-A3B card: `context_length` 64K→**131072**, sampling to "Thinking — General Tasks", `thinking_budget_tokens` 3K→32K, budgets up to **120K max/96K window**, harness timeouts bumped — PR #36. (3) `thinking_budget_tokens` calibration to 16K — PR #37 (later reverted). (4) Revert PR #37 after diagnosis showed sub-agent failures, not budget overruns, were the root cause — PR #38. (5) Swap sub-agent from `mlx-community/Qwen3.5-9B-8bit` to a second instance of Qwen3.6-A3B (`unsloth/qwen3.6-35-A3B-subagent`, Instruct preset, 16K, no thinking) — PR #39. Sub-agent layer measured: success rate 24% → **100%**, avg latency 39s → **22s**. (6) Primary `temperature` 1.0 → **0.6** after EVAL-2026-05-11-subagent-qwen36 showed three of four remaining failures were temp-variance driven — PR #40. Targeted re-run confirmed CP-05 and CP-24 fixed (CP-24 was previously suspected architectural). Remaining 2 known failures (CP-01 turn 2, CP-20 turn 1) are test-fragility — Qwen3.6 makes reasonable choices the tests forbid. Full eval progression: 33/37 (broken) → 34/37 (clean) → projected 36/37 with temp 0.6 now on main. Plan + diagnoses in `docs/superpowers/plans/analyze-the-results-and-immutable-lerdorf.md` and four `telemetry/evaluation/EVAL-2026-05-1X-*/COMPARISON.md` files. |
| **FRE-355: read primitive tail_lines (ADR-0068 D6)** | 2026-05-10 | `tail_lines: int \| None` on read executor. Seeks backward in 4 KiB blocks from EOF; bypasses `max_bytes` size gate; caps output at `max_bytes`. Resolves `current.jsonl` (19 MB) inaccessibility. 5 new tests. PR #33. |
| **FRE-356: self-telemetry.md skill doc (ADR-0068 D7)** | 2026-05-10 | New `docs/skills/self-telemetry.md` — 43-keyword frontmatter, 5 canonical bash+curl patterns (token stats, cache hit rate, cost by model_role, interaction outcomes, per-trace latency). PR #32. |
| **FRE-353: ES index template reconcile (ADR-0068 D4)** | 2026-05-10 | Deleted 4 dead field declarations; added 8 explicit typed mappings; fixed 3 type mismatches (`total_tokens` int→long, `latency_ms` float→long, `cost_usd` float→double). PR #31. |
| **FRE-351/352/354: Cloud emit parity + step rename + skill-doc fix** | 2026-05-10 | `litellm_request_complete` field parity; executor step renamed to `llm_step_completed`; `query-elasticsearch.md` run_python import bug fixed. PR #30. |
| **FRE-348: Reflection surfacing in context assembly (ADR-0067 G2)** | 2026-05-10 | `captains_log/recall.py` + context injection of up to 3 relevant reflections. 12 tests. PR #29. |
| **FRE-347: session_summary generation (G1)** | 2026-05-09 | `second_brain/session_summary.py` — LLM-generated session summary on every consolidation pass. 10 tests. |
| **FRE-213: Seshat owner identity (ADR-0052 amended)** | 2026-05-09 | Owner identity primitive; `user_id` anchor; operator stanza; `get_or_provision_user_person()`. 26 tests. PR #28. |
| **FRE-214: VPS topology audit + Wave D plans** | 2026-05-09 | Ratified full-harness-on-VPS; 30-row parity matrix; 3 Sonnet-ready impl plans; ADR-0045 amended. |

*Older items archived → `docs/plans/completed/2026-05-10-completed-archive.md`*

---

## Active ADRs

| ADR | Title | Status |
|-----|-------|--------|
| **0077** | **Artifact Draft — Sub-Agent HTML Generation** | **Implemented 2026-05-27 (PR #84, verified live)** |
| **0076** | **Adaptive Constraint Governance Protocol** | **Proposed (Codex-reviewed 3 passes, all blockers resolved). FRE-389 Approved. Depends on ADR-0075 ✅; concurrent with FRE-392.** |
| **0075** | **WebSocket Transport + Durable Channel** | **Implemented 2026-05-27 (FRE-388, PR #83 + 8 hotfixes, verified live on iPad)** |
| **0070** | **Output Channel Model — Markdown for Agents, Rich for Humans** | **Implemented 2026-05-21 (FRE-368 PRs #66 + #67); D8 measurement window open, review gate ≥ 2026-06-04** |
| **0069** | **R2-Backed Artifact Substrate** | **Implemented 2026-05-17 (FRE-227 + FRE-371); FRE-368 ✅; FRE-369 next consumer** |
| **0068** | **Agent Self-Telemetry Data Plane** | **Accepted 2026-05-10; all 7 follow-ups shipped (FRE-351–356)** |
| **0067** | **Reflection Surfacing in Context Assembly** | **Accepted 2026-05-10 (FRE-348); eval → FRE-350** |
| **0066** | **Skill Routing Defaults + Threshold + Feedback Loop** | Accepted 2026-05-07; D1–D5 all implemented; eval complete (Wave J) |
| 0065 | Cost Check Gate — Atomic Reservation | Accepted + Implemented; FRE-311 parked |
| 0063 | Primitive Tools & Action-Boundary Governance | Accepted + Implemented (P1–P6 complete via FRE-260/261/262/263/264/265) |
| 0061 | Within-Session Progressive Context Compression | Accepted + Implemented (FRE-251) |
| 0060 | Knowledge Graph Quality Stream | Accepted + Implemented; Phase 2 flag-gated → FRE-299 flip |
| 0052 | Seshat Owner Identity Primitive | Accepted (amended 2026-05-09) + Implemented — Wave E follow-ups FRE-342/343/344/345 |
| 0041 | Event Bus — Redis Streams | Accepted; Phases 1–4 live |
| 0040 | Linear as Async Feedback Channel | Accepted; Phases 1–2 live; Phase 3 → FRE-183 |

*Full ADR list: `docs/architecture_decisions/`*

---

## How This File Works

- **Linear is the task tracker** — this file tracks priorities and sequencing only.
- **Next task**: pick from "Immediately Actionable" table above, highest priority first.
- **Update after every ship**: add to Recently Completed, bump Last updated.
- **Specs** → `docs/specs/` · **ADRs** → `docs/architecture_decisions/` · **Session plans** → `docs/superpowers/plans/`
- **Archive** → `docs/plans/completed/` (items older than ~1 week)
