# Completed Archive — May 11–22 2026

Items graduated from the MASTER_PLAN.md Recently Completed section on 2026-05-30.

---

| Item | Date | Summary |
|------|------|---------|
| **FRE-376 Phase 1: end-to-end traceability** | 2026-05-22 | Schema migration `0004`: purged 4,077 NULL-trace `api_costs` rows, flipped `trace_id` NOT NULL, added `session_id`. `CostTracker.record_api_call` requires `trace_id: UUID`; `MissingIdentityError` added. `SessionRepository.append_message` validates model attribution. LiteLLMClient + service.app + Redis consumer all thread identity. 11 new unit tests, 2542 pass. |
| **FRE-368: agent artifact tools + PWA inline cards** | 2026-05-21 | PRs #66+67. `artifact_write`/`artifact_list`/`artifact_read` tools (R2ArtifactStore + Postgres, 5 MB cap, pgvector embeddings). Public `GET /api/v1/artifacts` + card telemetry endpoint. `ArtifactCard` inline cards, `ArtifactViewer` sandboxed iframe, `/artifacts` route. All ACs verified live on iPad. ADR-0070 D8 gate ≥ 2026-06-04. |
| **FRE-227 + FRE-371: R2 artifact substrate** | 2026-05-17 | PRs #62-65. Postgres `artifacts` table + migration 0003; `R2ArtifactStore` aiobotocore wrapper; `notes_write`/`notes_search` tools; `/internal/artifacts/{id}` endpoint; CF Access JWT verification. 45 unit tests. Private repo: R2 bucket + Workers Paid + dedicated CF Access app. Verified end-to-end on iPad. |
| **FRE-365: cache diagnosis + compressor upgrade** | 2026-05-16 | PR #61. Empirical diagnosis: gpt-5.4-nano doesn't participate in OpenAI prompt caching; compressor prompt was structurally uncacheable at ~188 tokens. Fix: compressor system prompt expanded to ~1065 tokens (3 worked examples + anti-patterns). Background roles rebalanced: compressor + entity_extraction → gpt-5.4-mini; captains_log + insights → claude_sonnet. |
| **FRE-366: SearXNG weather crash fix** | 2026-05-16 | PR #60. Strip leading `weather ` token from queries targeting weather engines (wttr.in, openmeteo, duckduckgo_weather). Add `X-Forwarded-For: 127.0.0.1` to suppress botdetection log noise. 6 new tests. Verified live: wttr/openmeteo return results cleanly. |
| **FRE-370: CF Access session_duration → 720h** | 2026-05-15 | Terraform apply (private repo). Six CF Access resources updated: session_duration 24h/1h → 720h; `auto_redirect_to_identity=true` on three apps. Eliminates daily re-auth across all CF Access-gated apps. |
| **ADR-0069 + ADR-0070: artifact specs** | 2026-05-15 | Two ADRs in Proposed status. ADR-0069: R2 (EU) + Worker + Postgres `artifacts` table + aiobotocore. ADR-0070: three output channels (agent↔agent markdown, agent→human-conversational hybrid, agent→human-persistent HTML in R2). Dual-representation refactor deferred to FRE-368 D8 measurement. |
| **FRE-343: personal time-window retrieval** | 2026-05-14 | PR #57. `(:Person)-[:PARTICIPATED_IN]->(:Turn)` provenance edge. `recall_personal_history(days_ago, topic?, limit?)` native tool scoped to `ctx.user_id`. `personal-history-recall.md` SKILL.md. `TaskCapture.user_id` tightened to non-optional UUID. One-shot backfill. |
| **FRE-315: Mermaid rendering + iOS follow-ups** | 2026-05-14 | PRs #53+56. `MermaidBlock.tsx` with mermaid.js v11; custom theme; figure/source toggle; fallback on parse failure. `docs/skills/mermaid-diagrams.md` skill. `Dockerfile.gateway` gains mmdc. iOS follow-ups (PR #56): SW network-first; C4 `xlink:href` fix; SVG/PNG download + copy actions. |
| **FRE-326: host-resource gate re-evaluation** | 2026-05-14 | Investigation over 8-day ES window: zero `consolidation_skipped_cpu_high`/`_memory_high` — gates are correctly dormant under remote-inference deployment. Verdict: retain as deployment-conditional safety valve for local-inference mode. ADR-0041 §Update 2026-05-14. |
| **FRE-363: operator stanza never set in gateway path** | 2026-05-13 | PR #51. `get_owner_stanza()` was only called in the unreachable non-gateway branch. Fixed: operator stanza populated in the gateway execution path using global `memory_service`. |
| **FRE-344: display_name config-driven seeding** | 2026-05-13 | PR #50. `AGENT_USER_DISPLAY_NAMES_JSON` seeds `users.display_name` (Postgres) and `:Person.name` (Neo4j) at startup. Coalesce: only overwrites NULL or email local-part. All 4 CF Access users seeded. |
| **FRE-299: flip graph quality governance gate** | 2026-05-13 | Set `AGENT_GRAPH_QUALITY_GOVERNANCE_ENABLED=true` after 14-day Phase 1 validation (33 anomalies, 0% false-positive). |
| **FRE-341: prune execution-service gateway token** | 2026-05-13 | Deleted stale `execution-service` token block from `config/gateway_access.yaml`. ADR-0045 confirmed the role was never used. |
| **FRE-337: skill nudge injection + XML headers** | 2026-05-13 | PRs #48+49. Two XML directive blocks appended after skill content (`<skill_index_directive>`, `<skill_usage_directives>`). `nudge:` frontmatter field. Feature-gated via `AGENT_SKILL_NUDGE_ENABLED`. ADR-0067 accepted. Eval: Family A 5/5 live tool use (was 0/5). |
| **FRE-328: capability-gap capture pipeline** | 2026-05-12 | PRs #43-47. `read_skill` emits `missing_skill_requested`; DSPy reflection populates `missing_skill_names`. ES aggregation + Linear ticket auto-creation when count ≥3 / ≥2 sessions. Parked: naming-stability problem (reflection model names same gap differently). Gate reset 2026-05-26 with agent-noun taxonomy. |
| **FRE-265: delete legacy tool modules** | 2026-05-12 | PR (post-#42). Removed 7 tool modules + 6 test files + governance entries + `legacy_tools_enabled` setting. Net −20 ruff / −10 mypy errors. ADR-0063 fully closed. |
| **FRE-342: Person dedup excludes user_id-bound nodes** | 2026-05-12 | PR #41. `_find_similar_entities` Cypher gains `AND node.user_id IS NULL` — prevents extracted "Alex" from merging into the owner `:Person` anchor. |
| **Post-PR-#34 eval stabilization** | 2026-05-11 | PRs #35-40. ES keyword fix; context_length 64K→131072; thinking_budget 3K→32K; sub-agent swap to second Qwen3.6-A3B instance (success 24%→100%, latency 39s→22s); temperature 1.0→0.6. Eval progression: 33/37→36/37. |
| **FRE-355/356/353/351/352/354: ADR-0068 chain** | 2026-05-10 | PRs #30-33. `tail_lines` on read; `self-telemetry.md` skill; ES index template fix; cloud emit parity; step rename; skills import bug. ADR-0068 fully shipped. |
| **FRE-348: reflection surfacing in context assembly** | 2026-05-09/10 | PR #29. `captains_log/recall.py` + up to 3 relevant reflections injected. 12 tests. |
| **FRE-347: session_summary generation** | 2026-05-09 | `second_brain/session_summary.py` — LLM-generated session summary on every consolidation pass. 10 tests. |
| **FRE-213: Seshat owner identity** | 2026-05-09 | PR #28. Owner identity primitive; `user_id` anchor; operator stanza; `get_or_provision_user_person()`. 26 tests. ADR-0052 amended. |
| **FRE-214: VPS topology audit + Wave D plans** | 2026-05-09 | Ratified full-harness-on-VPS; 30-row parity matrix; 3 Sonnet-ready impl plans; ADR-0045 amended. |

*For items before 2026-05-10 → `docs/plans/completed/2026-05-10-completed-archive.md`*
