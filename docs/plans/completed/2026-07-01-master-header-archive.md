# MASTER_PLAN header archive — through 2026-07-01

Verbatim snapshot of the MASTER_PLAN `Last updated` header before the 2026-07-01 trim. Kept for the decision trail; the live header is current-state only. `git log` has per-PR detail.

> **Last updated**: 2026-07-01 (master — **🟢 FRE-638 DONE (PR #300, 94144bf) — ADR-0098 D2 living-Claim substrate; first-write-wins RETIRED for Personal+Stance.** Native HAS_STANCE edges + bitemporal HAS_FACT Claims; pure `supersession.py` adjudicates FRESH/SUPERSEDE(correction|evolution)/REJECT (weaker/stale never clobbers, losers retained — NOT last-write-wins). Verified: parameterized Cypher (no injection), owner-sentinel skip-if-absent, bitemporal non-overlap, identity threaded; **AC-1/2/5 proven on live Neo4j :7688** (5 integration tests). Entity-desc FWW left intact (FRE-375 guard; World→Claim = FRE-711); follow-ups FRE-711/712/713. **NO deploy — inert until FRE-637+638 deploy together; seam FRE-642.** **⚠️ DEPLOY COUPLING:** main now carries TWO undeployed activate-on-rebuild chains — **vision** (FRE-661+664, upload-invisibility until 666) AND **memory** (FRE-637+638, new Neo4j Claims write-path). The NEXT `seshat-gateway` rebuild activates BOTH — ask-class, needs careful post-deploy verification (joinability probe + inspect first Claims + no memory corruption). Plan the deploy deliberately; don't let either ride an unrelated rebuild. **🟢 #300/#301 + #297/#299 all gated this session (5 PRs).** **🟢 FRE-664 DONE (PR #299, 362b1d3) — ADR-0101 T2 content-widening.** Message.content str→str|list[dict] (widening, backward-compat), shared malformed-safe accessor, executor sites routed, block-aware token estimator, real no-think bugfix; behavior-preserving for string content until image blocks exist (FRE-666); codex-reviewed, e2e test asserts blocks reach request_messages. **FRE-709 filed (Needs Approval)** — remaining gateway stringify sites to harden before FRE-666. **DEPLOY HELD.** Vision chain: 661✅→664✅→**665/666 next**→669 seam. **🟢 #301 turn_session_artifact DONE + VERIFIED LIVE (7184209)** — 13→5 panels (UI-built, vizType present), real-data scrutiny PASS (gate 13 committed/1 other, 10 verified envelopes, session costs ~$2). **🟢 #297 reflection_insights DONE + VERIFIED LIVE (6000d47)** — 4 panels incl. the owner-required proposal-detail table (1801 real docs, expandable to what/why/how/rationale). **🟢 FRE-494 DONE (PR #298, b78381d) — recall ADR pair landed.** ADR-0103 (Accepted, recall-no-clean-floor principle) + ADR-0104 (Proposed, multi-path retrieval + RRF), docs-only, codex 2 rounds; children **FRE-705/706/707 Needs Approval** (705 seam owner). Parent FRE-435 stays In Progress. **🟢 PR #297 (reflection_insights) DONE + DEPLOYED + VERIFIED LIVE (6000d47).** 4 panels: proposals by status/category (status/category mapping-drift fixed via additive runtime fields), proposed-changes-over-time, + the owner-required **proposal detail table** (saved-search, 1,801 real docs; columns title/category/status, rows expandable to what/why/how/rationale). Drift resolved (kibana pushed a 2nd commit committing the table + search object). Render-check green (cache-bust needed a full page reload — hash-route bust insufficient). Surfaced `~1800 awaiting_approval vs 6 approved` (corroborates FRE-708) + per-turn reflection titles (corroborates FRE-710). **🆕 Self-improvement thread (owner-surfaced, from the reflection_insights audit): FRE-708 Approved (HIGH, Opus)** — unify Insights Engine + Reflection into ONE pipeline (pluggable sources; already share model + promotion.py), model the relational layer in a graph **ISOLATED from the User memory KG** (System-class ≠ user knowledge; lighter store TBD, no geoloc, vectors TBD), observable funnel + closed loop; design-first → adr. **FRE-710 Needs Approval (Sonnet)** — reflection fires per-turn (executor.py:1658; 1872 refl / 2703 turns ≈ 0.7/turn) → move to per-session/event cadence (cost + funnel-noise); relatedTo 708. **FRE-704 Needs Approval (Opus)** — ES silent-field-drop. **New standing principle (memory):** System KG isolated from User KG. — **🟢 FRE-703 loop PR #296 GATED + DEPLOYED + VERIFIED LIVE (insights_engine, f2f7b6f)** — rebuilt via UI + fixed a *silent mapping-drift bug*: `insight_type` is `text` on old `agent-insights-*` shards, `keyword` on new → aggregating either way silently dropped shards (renders-but-misleads). Fix = additive `insight_type_norm` **runtime field** on the shared `agent-insights-pattern` (guarded Painless coalesce, verified non-breaking — reflection_insights still renders clean). 3 value-focused panels (added actionable-vs-not, dropped empty anomalies). **Master real-data scrutiny PASS**: independent ES 30d = 465 docs, type 265/100/100, actionable 265/200 — matches the chart to the bar. Kibana-import (standing class). **⮕ OWNER GATE-REQ for the NEXT loop PR (reflection_insights, shares agent-insights-pattern): the redesign MUST include a TABLE showing the actual insights (per-row proposed-change/content text), not only aggregate charts — the current 4 panels are all aggregates and never surface WHAT an insight says. Master gates on the table's presence + real per-row content (owner-directed to kibana + master 2026-07-01).** **⛔ STANDING DEPLOY-HOLD (gateway): FRE-661 + FRE-664 are merged-but-undeployed on main and must NOT ride a `seshat-gateway` rebuild until the FRE-665/666 vision chain lands (or owner confirms uploads idle)** — retiring the attachment text-pointer makes uploads invisible to the model on any attachment turn until FRE-666 wires turn-assembly content blocks; uploads are live (FRE-369). Also undeployed on main: FRE-637's SLM extraction-prompt change (inert — stances/claims unconsumed until FRE-638). Both ride the *same* eventual post-vision-chain rebuild. **🟢 3 PRs GATED + MERGED THIS PASS (kibana-loop + build + build2):** (a) **FRE-637 DONE (PR #293, e49df35)** — ADR-0098 D5 extraction-emission contract (T1 head): every entity gets a knowledge class (fail-open to World), structured stances (owner→World, affect+mastery) + Personal claims, Python-stamped provenance (observed_at=turn time). Additive, 10 contract tests, no security surface, **no deploy** (inert until FRE-638). ADR-0098 closes at seam FRE-642, not here; FRE-638 unblocked. (b) **FRE-661 DONE (PR #294, 0ceb327)** — ADR-0101 ticket-1 structured AttachmentRef carrier (clean user_message, AC-5 byte-for-byte; processing_target allowlist-validated; reduces injection surface). **DEPLOY HELD (see above).** FRE-664/665/666 unblocked. (c) **FRE-703 delegation_outcomes retire-proposal (PR #295, deaccfb, docs-only)** — loop investigated raw events instead of hand-building a misleading chart: `delegation_package_created` **dormant since 2026-05-10** (17 docs ever), the "live signal" `delegation_pattern_analysis_*` carries its metrics in fields **ES silently drops** (confirmed independently: total_delegations/insights_found in `_source` but `_ignored`, unqueryable via `_field_caps`; template `total_fields.limit:300`+`ignore_dynamic_beyond_limit:true`), and **no outcome field exists** anywhere → no honest redesign possible. **⮕ BOTH OWNER CALLS RESOLVED (yes/yes, 2026-07-01): (1) `delegation_outcomes` RETIRED** (commit 2279039 — ndjson + import entry + doc listings removed, live Kibana dashboard+viz deleted, shared index-pattern preserved, telemetry-surface gate green 0-drift); **(2) ES silent-field-drop → FRE-704 filed** (Needs Approval, Tier-1:Opus, Telemetry Surface Audit — investigate raise-cap vs prune-dead-fields vs ignored-fields monitor; likely ADR-0090 amendment). FRE-703 umbrella stays In Progress (loop continues). **🆕 FRE-708 filed (Needs Approval, HIGH, Tier-1:Opus, Linear Async Feedback Channel/ADR-0040)** — surfaced by the reflection_insights redesign: the **self-improvement loop is open + blind**. Insights Engine + Captain's Log reflections produce signal (465 insights/30d, 265 actionable; 1,872 reflections) but insight→ticket linkage is unlogged (`linear_issue_id` populated on only **6/1,872** reflection docs, mostly since-canceled) → can't monitor conversion, can't return ticket-outcome value to the feedback loop. Scope: wire+log bidirectional linkage · observable produced→ticketed→shipped funnel (explicit ES fields per FRE-704) · close the loop (outcome→signal on source insight). Design-first, may extend ADR-0040/ADR-0030 → candidate for adr session. — **🟢 FRE-406 DONE + DEPLOYED + VERIFIED LIVE (PR #292, main 835a07c) — first kibana value-audit-loop PR (FRE-703).** Rebuilt both `prompt-cost-cache` Lens panels in the Kibana UI (real UI export, `visualizationType=lnsXY` present — the reopened render defect) + regression test `test_every_lens_has_visualization_type`; value redesign: panel-2 now charts cache-hit-rate `cache_read/(cache_read+input)` over time (answers FRE-406's own <60% future-gate), not a hash-count. **Kibana-import deploy (standing class) + live render-check green** (Playwright on cloud-sim-kibana: no "Visualization type not found", 2 render-complete, real callsites in legend). **Master real-data scrutiny PASS** (independent ES 30d: orchestrator.primary $28.09/51.7% cache-hit · role.sub_agent $4.53/8.0% · role.primary $1.52/**flat 0%**) — bars/lines match to the cent. Loop continues in the kibana session; next PR gated as it lands. **Trend note (not a ticket):** orchestrator.primary cache-hit 51.7% is under FRE-406's 60% composer-redesign gate, but that lever already shipped as the ADR-0081 cache chain (cross-turn reuse 0→8k+); role.primary's flat-0% is the only genuinely-new anomaly, low-cost ($1.52) — surfaced, not filed. — **🔴 RE-PRIME POINTER (context reset at clean boundary):** (1) **FRE-593 DONE** (PRs #289/#290/#291) — context-occupancy backend live + v2 dashboard verified against real data; **`create-visualization` skill graduated** (`.claude/skills/create-visualization/`, build+scrutinize+iterate, docs-first + cache-bust rules) + build's Playwright technique note (`docs/research/2026-07-01-kibana-lens-playwright-build-findings.md`). (2) **🔁 DASHBOARD VALUE-AUDIT LOOP IS LIVE** in the **kibana** worktree session (Sonnet, self-paced) — umbrella **FRE-703** (Approved) + worklist `docs/research/2026-07-01-dashboard-value-audit-worklist.md` (16 dashboards; retire NOTHING up front — verified all 3 premature retire-candidates alive); loop rebuilds one dashboard/PR via the skill, **stops at PR** — **master gates each PR** (real-data scrutiny; owner makes the "useful" call). First PR expected = **FRE-406** (prompt-cost-cache, broken Lens). (3) **Sessions ALL IN MOTION as of 07-01 — do NOT re-dispatch; gate their PRs as they land:** build→**FRE-637** [Opus, P1 ADR-0098 structure] · build2→**FRE-661** [Sonnet, P3 vision head] · adr→**FRE-494** (re-scoped 07-01: recall = multi-path retrieval; author **ADR-A principle FIRST** → **ADR-B multi-path/RRF architecture**; children to file = design spec · FRE-655 re-scope · structure-wiring[gated on 637]; from FRE-700 doc `2026-06-30-recall-as-retrieval-and-the-dual-domain.md`) [P2, Opus]. **⮕ On re-prime: `gh pr list` for the kibana loop's dashboard PRs and gate them.** Prior header follows. — **🟢 FRE-698 SHIPPED + DEPLOYED + VERIFIED LIVE (PR #288) — joinable reranker telemetry (ADR-0074).** `reranker_applied`/`reranker_failed` now carry trace_id/session_id/task_id + per-call span_id + enriched fields (model_id, candidate_count, input_cap, result_count, top_score, latency); gateway→SLM `X-Trace-Id` headers (gated on trace_id). **Codex HIGH fixed:** the recall paths now feed session_id into query_memory (FRE-673 pattern, at memory_search + executor entity-path) so the ADR-0074 probe actually joins. Additive log fields only (no schema/index change). **Deployed + live-verified** (trace 902d7786: 3 reranks, distinct span_ids, all enrichment fields incl. top_score; ADR-0074 probe green / zero orphans). SLM-side half = **FRE-701 DONE** (cross-repo slm_server PRs #7/#8 — join now end-to-end). **⮕ unblocks FRE-699** (needs this telemetry LIVE). **🟢 FRE-700 DONE (PR #287, owner-closed as research-capture) — recall-as-retrieval research doc captured; the recall-architecture ADR + FRE-655 re-scope is PARKED as a separate future owner decision (not FRE-700)** — the recall-architecture ADR + FRE-655 re-scope + multi-path retrieval design are a focused owner follow-on (concept: recall = multi-path retrieval; structure separates on closed-vocab axes, semantic on the open axis; no clean floor → re-embed closed, probe is a regression instrument). **🟢 FRE-681 SHIPPED (PR #286) — ADR-0102 T1 license-clean PDF utility.** New pure module `documents/pdf_utils.py` (pypdfium2/Pillow: extract · density · rasterize · pixel-coverage · salience-select · page-subset); AC-3 license-clean (no AGPL/GPL — pymupdf/pdf2image/poppler excluded) + AC-5 anti-loophole selection, 38 tests; **pure + unwired (zero blast radius), no deploy** (deps install on a future rebuild; FRE-682+ wires it, gated on the ADR-0101 image chain). First tile of the ADR-0102 document chain. **🟢 RERANKER THREAD LANDED — prod reranker = MLX 4B mxfp8, deployed + verified live (FRE-695/696).** **FRE-695 (PR #284)** reranker-separation sweep: **NO retrieval model opens a clean floor** (best reranker Youden's J 0.785 @ 8B mxfp8 vs best embedder 0.59) — the floor problem is **structural (topical density), not the model** → clean separation must come from **graph structure** (validates ADR-0097/0098 KG); the reranker is the **soft FRE-655 signal** (~88%R/9%FP, never a hard cutoff). Also: **MLX runtime fixes the llama.cpp reranker stall**; embedder ceiling runtime/quant-robust (Q4 was the FRE-656 confound, not quant generally). **FRE-696 (PR #285) DEPLOYED + VERIFIED LIVE** (gateway rebuild, owner-approved): prod reranker `Voodisss/Qwen3-Reranker-4B` (llama.cpp, stalled under load → silent passthrough) → **`Qwen/Qwen3-Reranker-4B-mxfp8` (MLX, reliable)** + `reranker_input_cap` 50→25. End-to-end verified (trace 6c269a23: 2 search_memory queries → 2 MLX reranks, 200 OK, floor 0.0, 500→cap25→top10/5). 4B not 8B (latency curve: 2× faster, −0.038 J within noise; honors owner cap). **Open tail:** **FRE-697** ONNX-on-VPS reranker = always-on private failover/primary (Approved → `/build`) · **FRE-698** reranker telemetry UNJOINABLE — no trace_id on rerank events (ADR-0074 fix, Needs Approval, owner-flagged) · **investigation (unfiled):** reranker only fires on the `search_memory`/vector path (NOT `recall_personal_history`/proactive); multi-query recall doubles rerank cost — measure path-frequency + whether wider reranking helps, **gated on FRE-698 joinability**. **🟢 FRE-690 SHIPPED (PR #282) — ADR-0101 control-spine amendment; vision-chain HOLD now LIFTED.** ADR-0101 amended in place with **§8 shared control spine** — 8a per-attachment cloud/local override (fail-closed) · 8b cloud-vision pre-flight cost + ADR-0065 reservation + threshold-confirm + meter · 8c ADR-0074 joinability — plus §6 per-dimension guardrails, written **attachment-agnostic so ADR-0102 inherits it (build once)**. ADR-0101 stays **Proposed**. Re-ticketing done: **FRE-661/665/666 amended** (kept Approved, expanded scope) · **NEW Needs-Approval FRE-691** (image cost) · **FRE-693** (image joinability) · **FRE-692** (PWA override), all attachment-agnostic · **FRE-669 seam extended** to full AC-SEAM, blockedBy 691/692/693. Cross-wired so ADR-0102's **FRE-686 blockedBy 691 · 688 blockedBy 693 · 687 relatedTo 692** (0102 cost/joinability become reuse-plus-PDF). Doc-drift fixed: ADR-0102 Context line ("this ADR establishes the shared design") → now "inherits ADR-0101 §8." **⮕ The vision image chain (661→669 + spine 691/692/693) is design-complete + buildable — gated only on owner approving the 3 new spine tickets** (+ optional re-approval of the expanded 661/665/666). No deploy (docs). **🟢 FRE-694 SHIPPED (PR #283) — embedder separation ceiling: NO embedder opens a clean floor; the re-embed question is SETTLED.** Re-tested f16 across the board (0.6B/4B/8B + Voyage cloud SOTA) on the FRE-670 probe, measuring SEPARATION (positive/negative cosine overlap) not recall@5. Verdict: **no embedder — local or cloud SOTA — yields a clean cosine cutoff** (best Youden's J 0.42→0.53→0.53→0.59; even Voyage ≈ 80% recall @ 16% false-positive). **Q4 confound CONFIRMED + cleared** — the same 4B scored ≈0.6B at Q4 but J≈0.53 at f16 → *precision*, not size, was the suppressor (the earlier caveat was right). Size helps with **diminishing returns, plateaus at 4B** (8B buys nothing over 4B). **Dimension answer: more dims HURT** — every arm peaks at a *middle* dim (512–1024), native equal-or-worse (extra dims add noise, not signal); if ever re-embedding, truncate MRL to ~512–1024 (4–8× less storage, no quality loss). Parity gate held (Δ≤0.012) *after* catching a real cosine-space bug (Neo4j scores (cos+1)/2). **Recommendation (settles the FRE-656/671 embedder thread): do NOT re-embed for separation — recall already saturates at the 0.6B prod embedder; the clean-floor lever is the RERANKER (cross-encoder) downstream. FRE-655 floor calibration must expect an OVERLAPPING cosine distribution and lean on the reranker, not a clean cosine threshold.** Next = reranker-separation arm on the same probe (fold into FRE-671/655 or new ticket). No deploy (eval+docs). **🟢 FRE-670 SHIPPED (PR #281) — vocab-divergent semantic recall probe; AC2 PROVEN.** The FRE-489 gate was lexical-masked-as-semantic (BM25 won it); the new 54-case probe (owner-authored, imagery/paraphrase, query↔note Jaccard median 0.03, CI-enforced) flips it: **BM25 recall@5 0.659 ≪ vector 0.989 (0.6B) / 1.000 (4B)** on the isolated test substrate, with an instrument sanity check (same BM25 still 1.00 on the old probe → the drop is the probe, not the tool). **The probe now DISTINGUISHES embedders** (0.6B→4B recall@1 0.705→0.761; 4B closes the one imagery miss + narrows the register gap +0.092→+0.021) — which FRE-489 structurally could not. This substantially answers **FRE-656** (its 0.6B-vs-4B A/B is #281's 3-arm run). **Master rec (owner's call): HOLD at 0.6B — do NOT trigger the one-way 2560-dim KG re-embed.** The 4B edge is real but small and recall@5 is already ~0.99 (saturated at 54 cases); cost (irreversible full-KG re-embed, CPU-only VPS, embedder on the write path, FRE-659 zero-vector-on-outage prereq) outweighs a +0.056 recall@1 gain. Revisit on a GPU host (FRE-671) or a larger non-saturating probe. **CAVEAT (later 06-29): the 4B benchmark arm ran at Q4 — which perturbs the fine cosine geometry that *separation* depends on (recall@k stays robust), so the "4B doesn't help" / "hold 0.6B" read is PRECISION-CONFOUNDED, not settled. FRE-694 (Approved) re-tests precision-clean: 0.6B-f16 vs 8B-f16 (owner placed 8B-f16 on slm.example.com, replacing 4B-q4) + Voyage cloud arm, measuring SEPARATION (positive/negative cosine overlap + dimension sweep), not recall@5 — the binary question being "can any embedder open a clean floor, or is the lever the reranker." Prod embedder unaffected (VPS 0.6B, embeddings:8503).** No deploy (eval+docs). **🟢 ADR-0102 (Document PDF Ingestion) MERGED + ACCEPTED by owner (PR #280; FRE-667 Done).** Tiered text-first/vision-on-demand; **controls/cost/monitoring folded in** (§7: per-attachment cloud/local override fail-closed · pre-flight cost estimate + ADR-0065 gate + threshold-confirm · ADR-0074 trace/session/task joinability); license-clean stack (pypdfium2/Pillow; PyMuPDF-AGPL + Poppler-GPL rejected); 12 outcome-level ACs + AC-SEAM. Impl chain **FRE-681→689 Needs Approval** (Tier-2:Sonnet, project "Agent Vision and Attachment Ingestion"), seam **FRE-689** master-run; whole chain **HARD-depends on the ADR-0101 image chain landing first**. **ADR-0102 ACCEPTED (owner).** Of the impl chain, only **FRE-681 Approved** (license-clean PDF utility — independent, "can start early", dispatchable now); **FRE-682→689 stay Needs Approval** — dependency-gated behind the HELD ADR-0101 image chain, and the cost/override/joinability ones should reuse the ADR-0101 control spine the amendment will add (don't build it twice). **⮕ ADR-0101 controls/cost/monitoring amendment now UNBLOCKED** (owner-set: do it now that 0102 cleared the master gate) — ADR-0101 has only §6 Guardrails, no cost/joinability/egress-control; write the §7-equivalent ONCE in 0101 as the shared spine 0102 inherits. Master to draft the gap-list + new-ticket slate for the adr session; **vision impl chain FRE-661→669 stays HELD** (do-not-dispatch) until that amendment lands. **🟢 FRE-680 SHIPPED (PR #279) — delivery verification spine live.** `scripts/reconcile_board.py` (read-only board↔Linear↔merged-PR reconciler; verdict = 4 fields, UNVERIFIABLE≠PASS; exit 1 on drift) + direct-to-main push-guard hook (build/worktree blocked, primary/master allowed; activates on settings reload). First live run earned its keep — caught + cleared 3 stale-narrative drifts (FRE-369/491/655 — header lagged Linear, all Done); board now reconciles clean (exit 0, 22 PASS). **🟢 ADR-0101 NARROWED TO IMAGES + ADR-0102 (documents) CARVED OUT — PR #278 merged (owner-directed split).** Vision ingestion now scopes raster images only (current-turn); the categorically-different PDF/document problem (page count · scanned-vs-text-layer · OCR-vs-vision · chunking · per-page cost) → **ADR-0102 (Document Ingestion, forthcoming)**, reusing ADR-0101's foundation (FRE-661 carrier · credentialed store.get · content widening · capability routing). Restructure: **FRE-665** drops supports_pdf_documents (→ supports_vision only); **FRE-667** = ADR-0102 design-authoring [O], **owner-approved 2026-06-29 → adr session**; **FRE-669** seam → image-only (AC-6 dropped). FRE-369 live bug was an image → image fix ships ahead of document design. **ADR index (README) brought current** — 0090–0101 added, 0089→Implemented. **🟢 RECALL LATENCY FIXED — reranker→Mac-GPU flip DONE+LIVE (FRE-671 + FRE-679, deploy fbc2df5).** Owner reframed the fix: NOT "disable the reranker" (it IS the correctness layer; owner rejected that) — flip WHERE it runs. `config/models.cloud.yaml` reranker → Mac/laptop GPU 4B (`Voodisss/Qwen3-Reranker-4B` @ `slm.example.com`, CF-Access token injected, fails safe to vector+recency passthrough). **Live-verified 17-31s → ~6.2s** (reranker 15.7s→5.4s, recall intact, no passthrough, owner saw the rerank call in slm-server logs). Cap clarity: `recall_candidate_cap=500` (generation=candidate_set_size) vs `reranker_input_cap=50` (FRE-672, what's reranked — IS live). Further cut (cap 50→20) = recall@5 quality knob → **measure on FRE-670, don't guess**. **Embedder HELD on VPS** (owner): bit-identical f16 GGUF both sides (no re-embed) BUT embedder is on the WRITE path + **FRE-659** (zero-vector-on-outage) must land before moving it behind the tunnel. Method wins this thread: owner corrected master 4× (telemetry≠behaviour — agent recalled Crete fine via PARTICIPATED_IN; FRE-656 questions reranker cost but RECOMMENDS keeping it; "no-find" was SLOWEST not fastest because floor=0.0 reranks everything). **FRE-670 sharpened: 20 AUTHENTIC owner-co-written prod-mined queries (NOT model-alone), type-split, BM25-must-not-win** — 5 seeds harvested from the live session (Florian/Theo, Italy trip, Greek migration, legumes/health, cars/dealers). Prior — **🟢 RECALL WORKS LIVE — FRE-673 DONE (PR #276), telemetry-verified.** The real fix (PR #275 fixed the wrong path): build pulled prod trace 88164bbf → live recall path is the agent's **`search_memory` tool**, whose `TraceContext` had `user_id` but no `authenticated` field → recall ran unauthenticated. Fix = add `TraceContext.authenticated` (propagated through new_span) + thread it search_memory→query_memory. **Deployed + LIVE-VERIFIED:** on an indirect/semantic query (Greek migration), `memory_recall` flipped **candidate_set_size 0→14, empty_result True→False, result_count 0→10, recalled_token_count 0→399**. "No prior discussions" is genuinely resolved. Methodology lesson (memory): pull the real prod trace, don't fix the obvious-but-wrong path. **NEW: 🟠 latency — recall took 17.3s, dominated by a 15.7s VPS-CPU reranker pass (14 docs ≈1s/doc) → FRE-679 [perf, options: lower input_cap / disable reranker / async / GPU-host=FRE-671 now with live evidence].** Also surfaced this thread: **FRE-678** (recall identity+transport convergence — one fail-closed RequestContext, single chokepoint, SECURITY audit of the 24-reads-vs-10-filters gap + visibility-vs-PARTICIPATED_IN dual authz models) · **FRE-674** (deferred PRIVATE-scope classification) · **FRE-675** (responses don't truly stream — token-delta path scaffolded, never wired) — all Needs Approval. **Recall-quality reframe: the agent already recalled well via the PARTICIPATED_IN tool path; search_memory was a duplicate — FRE-678 decides which model wins.** Prior — **LIVE VERIFY FOUND THE DEEPER BUG → FRE-673**. Owner tested recall live (indirect/semantic queries); the new `memory_recall` telemetry showed **candidate_set_size=0 / empty_result=True on EVERY recall** despite `vector_entity_count=20` at top_score 0.77. Root cause (confirmed in code + Neo4j): **`executor.py:2035-2044` builds MemoryQuery + calls query_memory WITHOUT user_id/authenticated**, so recall runs unauthenticated; **ALL prod memory is `visibility='group'`** (2145/2145 turns, 7392/7392 entities) which the visibility filter reveals only when `authenticated=true` → the filter drops 100% of memory. **This is the dominant "no prior discussions" cause now that recency is fixed — pre-existing, exposed by the de-gate's telemetry (observability did its job).** De-gate stays ON (correct, harmless, masked until 673 lands; fix unblocks it). Proactive-suggest path threads auth → why agent had *any* memory. **Next: approve FRE-673 → fix → re-verify recall returns turns.** **🟢 RECALL DE-GATE ROLLED OUT LIVE (owner-approved)** — `AGENT_RELEVANCE_BOUNDED_RECALL_ENABLED=true` set in `.env`, floor 0.0 (recall-first), gateway rebuilt cloud-side + healthy, clean startup, no recall/reranker errors. The deterministic 30-day "no prior discussions" gate is **OFF in prod**. Migration-0012 gate already cleared. **Verification COMPLETE — FRE-655 Done 06-28**: owner live test (ask about a >30-day-old topic → should surface) + confirm `memory_recall` events fire (AC-6) + latency stays low (FRE-672 top-N bounds the reranker). FRE-655 closes on live verify → then **ADR-0100 Accepted + FRE-435 close**. Reversible: unset the flag + rebuild. Prior 06-28: **FRE-672 SHIPPED + Done (PR #274)** — reranker bounds input to top-N by vector score (`reranker_input_cap=50`); kills the ~4.4s CPU-reranker latency the de-gate would otherwise add per turn. Back-map verified (rr.index→rerank_indices→correct turn_id), prod-inert until rollout (legacy candidate sets <50 = behavior-preserving), rides the rollout rebuild. **⮕ The ADR-0100 / FRE-655 recall rollout is now LATENCY-SAFE — last blocker cleared.** Next = flip the de-gate flag (floor 0.0) + deploy + A/B verify (recall@5 preserved + reranker time drops = the end-to-end proof). Owner-involved seam (FRE-655), master deploys. Prior 06-28: **ADR-0101 Agent Vision Ingestion merged Proposed (PR #272)** — design pass for the FRE-369 image-blindness gap (agent gets a CF-Access login page, not the image): turn-assembly resolution + server-side credentialed `store.get` R2 fetch (bypasses CF Access) + structured attachment carrier (folds FRE-661) + capability-driven routing (supports_vision flag, **fail-closed** on local, no silent non-vision fallback) + content blocks (raster + PDF) + fix artifact_read public_url honesty. 5 alternatives, 8 fail-able ACs, codex 2 rounds. **FRE-662 (design) Done.** New Linear project **"Agent Vision and Attachment Ingestion"** (governed by ADR-0101): impl chain **FRE-661→664→665→666→667→668 + seam FRE-669** [mostly S, seam O], all **Needs Approval**; FRE-669 = live-smoke seam owner (master gates). Prior 06-28: **Memory-recall DIRECTION locked (owner)** — the FRE-489 probe is **keyword-search-masked-as-semantic** (cases share vocabulary with stored text), so BM25 beating vector in FRE-656 is a **probe flaw, NOT a verdict**. **Vector GraphRAG stays the committed path** (SOTA-aligned); retrieval is a **toolkit applied appropriately** (lexical/vector/reranker/recency/entity — hybrid routing, not a single winner). Locked: **FRE-672 [S, Approved] rerank top-N by vector score (not all 500)** = the rollout unblocker (kills the ~4.4s CPU-reranker latency on the VPS, no GPU-host dependency); **FRE-670 reframed** → a *real semantic eval* (messy/oblique/real communication, measures tool-appropriateness); **FRE-671 PARKED, evidence-gated** (GPU-host = hot-path availability dependency; decide on data·risk·value AFTER FRE-670, NOT on the broken probe). **Rollout sequence: FRE-672 → flip flag (floor 0.0, recall-first) → live verify.** Memory: `project_memory_recall_direction_2026_06_28`. Prior 06-28: **FRE-656 embedder benchmark SHIPPED + Done (PR #273)** — eval+docs + small enabling src (CF-Access token + WAF-UA + per-endpoint keying in embeddings/reranker, hostname-gated, prod-inert, 25 auth tests pass; focused review). **Findings: (1) 4B = NO gain over 0.6B** (recall@5 0.722 both, J=0.714 both); **(2) the FRE-489 probe is the wrong instrument** — a BM25 keyword baseline BEATS the vector path (1.00 vs 0.72) because the probe shares vocabulary with stored text → tests LEXICAL not SEMANTIC recall, can't distinguish embedders or settle the floor; **(3) one win: reranker hosting** Mac-GPU ~0.6s vs VPS-CPU ~4.4s. **Rec: do NOT re-embed prod; build a semantic probe (FRE-670) first; reranker hosting → FRE-671; floor stays weak for both.** **⮕ OPEN OWNER DECISION — the FRE-655 recall rollout:** the de-gate works *independent* of embedder (0.00→0.72) and the rec floor is 0.0 (recall-first), so the rollout can proceed NOW with 0.6B + floor 0.0, OR wait for FRE-670. Follow-ups: **FRE-670** (semantic probe) · **FRE-671** (reranker hosting). Prior 06-28: **FRE-369 user-uploads MERGED (PR #269)** — presigned-PUT→R2 upload UX (backend presign/complete + migration 0012 `upload_pending` + PWA file-picker/drag-drop/paste). **Bounced once, hard**: round-1 deep code-review (51-agent workflow) caught a FEATURE-BREAKING bug (presign signed ContentLength as SigV4 exact-match → 403 on every real upload, hidden by a fully-mocked store) + 5 silent-data-loss blockers; round-2 fixed all + added a presign contract test. #8 (attachments-as-prose pollutes memory extraction) → **FRE-661** Needs-Approval (persisted msg already clean). **FRE-369 Done (deployed+verified 06-28).** **DEPLOY = ask-class, owner-gated, MIGRATION-FIRST** (apply 0012 → verify column → gateway rebuild → PWA rebuild → smoke test); build runbook had gateway-before-migration (would 500 artifact reads) — master corrects order. Prior 06-28: **FRE-655 measure+propose subset SHIPPED (PR #271)** — ADR-0100 assembled-seam A/B + floor calibration (eval+docs only, no src/). **Recall mechanism PROVEN end-to-end on the FRE-489 probe: entity recall@5 0.00→0.72 (13/21 recovered), broad-path 0/21→18/21** — "no prior discussions" removed; validates FRE-653/654. **FRE-655 (Done 06-28; kept open at the time)** — rollout deferred by owner: the 0.6B embedder's positive/negative cosines overlap (no clean floor separator), owner verbatim *"test a better embedding model first; why implement now on scores that are indecisive."* **FRE-656 (embedder benchmark) promoted follow-on→PREREQUISITE of the rollout** — **Approved (owner 2026-06-28), blockedBy FRE-655 removed, dispatchable now [O]**; it re-runs this A/B+calibration per candidate embedder + a KG re-embed (candidate research: Qwen3-Embedding-4B/Snowflake arctic-embed-l/Nomic v1.5/BGE-M3). Flag stays default-off (FRE-653/654 inert, zero prod risk). FRE-655 Done — flag went live+verified post-embedder; FRE-435 after that. Research doc: `docs/research/2026-06-28-fre-655-relevance-bounded-recall-ab.md`. Prior 06-28: **FRE-654 SHIPPED (PR #270, Done)** — ADR-0100 broad-path seam (`query_memory_broad` / MEMORY_RECALL intent): adds `query_text` (threaded protocol→adapter→context→executor) + vector-relevant entity candidates across all time, 90-day cutoff demoted to a ranking signal; flag-gated default-off (prod no-op). **Both recall paths now land.** Focused master review (all 6 FRE-653 defect classes checked clean — ordered cap, floor-on-ranking, scoped on query_text; MATCH-only, parameterized) + AC-1b control/proof run green on :7688 (38 tests, no regression); merged on focused pass not the 40-agent workflow (smaller/dormant; FRE-655 A/B is the backstop over both paths). **No deploy** (deferred to FRE-655). **FRE-655 now UNBLOCKED** (assembled seam: A/B on FRE-489 + floor/cutoff calibration WITH OWNER + flag rollout + live verify) — owner-involved; gateway rebuild + additive-ES-template ride along at the flag-flip. Prior 06-28: **FRE-653 SHIPPED (PR #268, Done)** — ADR-0100 core de-gate on `query_memory`, flag-gated default-off (prod no-op until FRE-655 flips it). **Bounced once then merged**: round-1 master review (high-effort 40-agent code-review workflow + security + AC runs) caught **6 confirmed flag-on correctness defects** the green AC tests structurally couldn't (single-seed) — incl. an unordered `candidate_cap` slice that reintroduced the very "no prior discussions" miss; round-2 fixed all 6 + added the **multi-turn crowding proof test** (master ran it green on :7688) + promoted caps to config; `recency_days`-under-flag contract documented + caller-audit tracked as **FRE-658**. **No deploy** (dormant flag; gateway rebuild + additive-ES-template registration deferred to FRE-655's flag-flip). **FRE-654 now unblocked** (broad-path seam; build kept context). Follow-ups Needs-Approval: **FRE-657** (identity-hook false-positive), **FRE-658** (explicit-window recall semantics). Prior 06-28: **ADR-0100 Memory Recall — Relevance-Bounded Candidate Generation merged Proposed (PR #267)** — the routed Phase-2 recall fix (FRE-494): replace recency-keyed candidate generation with **vector top-k over `entity_embedding` across all time** + a calibrated config-driven `recall_similarity_floor`; recency demoted to a ranking weight; returned set sorted by combined relevance (fixes a 3rd defect — scores computed but discarded for ordering). **Answers the owner's volumetry/perf constraint**: top-k bounds the candidate set (scale-invariant), staleness delegated to ADR-0098 Claims. Flag-gated default-off → A/B on FRE-489 → live verify (FRE-433 discipline). **ADR-0100 Accepted (owner) + impl chain Approved**: **FRE-653** core de-gate [S] → **FRE-654** broad-path seam [S] → **FRE-655** assembled SEAM (A/B + floor/cutoff calibration w/ owner + rollout + live verify) [O] — all **Approved → build**; **FRE-656** embedder/reranker benchmark [O] held Needs-Approval (gated behind 655). **Seam owner = FRE-655**; ADR-0100 closes only when 655 proves AC-1a…AC-7 live with the flag on, then FRE-435 can close. Doc-drift reconciled: **FRE-435 reopened In Progress** (was prematurely Done 06:47 — Phase 2 designed, not shipped); **FRE-493 closed Done** (research doc on main via FRE-491 + recommendation realized in ADR-0100). **FRE-491 closed Done** (owner signed off the V6 reframing: recall failure is structural — the verified hard 30-day recency-gate — so numeric cutoff calibration is unneeded; H4 dominant gate named with code proof; carve-outs FRE-646/490/647 remain; parent FRE-435 stays In Progress). **Highest-leverage next move = recency de-gate (Phase-2), to be framed in adr.** **FRE-631 + FRE-634 Canceled** (owner-approved, redundant with the Approved ADR-0098 wave): 634 = first-write-wins correctability, fully carried by **FRE-638** (retire-FWW + Claims/bitemporal); 631 = ADR-0071 curation gate, but **ADR-0071 is Superseded** (its accept-precondition is unsatisfiable) and the gate is carried by **FRE-639 + FRE-638**. ADR-0071 file status confirmed already Superseded-marked. Prior 06-28: **ADR-0099 Configuration Management & Validation Accepted (owner; PR #266)** — single-source role matrix + tiered validator + profile-divergence policy; impl chain **FRE-648→649→650 Approved** (648 audit=head · 649 guard+drift-correction[local nano→mini] · 650 generative loader), **651/652 to follow** (seam = FRE-652; **FRE-649 is an owner-visible local config change: entity_extraction nano→mini, captains_log/insights →sonnet**). **FRE-645 eval-fidelity guard shipped (PR #265, Done)** — harness pinned to prod model-config + divergence guard; first instance of ADR-0099. **Worktree-branch hygiene permanently fixed**: rebase-before-PR (build/adr) + delete-on-merge (master) + GitHub "Main" ruleset (block force-push/delete) + auto-delete-head-branches on. Earlier 06-28: **FRE-491 recall baseline integrated (PR #264).** The owner's "no prior discussions" symptom = a **verified deterministic 30-day recall recency-gate** (`recency_days=30`, `protocol.py`→`service.py`) upstream of vector/reranker — **model-independent, deterministically fixable**; H4 query-layer is the dominant gate, embeddings + write-path are NOT the bottleneck. **Highest-leverage next move = recency de-gate (Phase-2, owner flagged possible urgency).** FRE-491 Done (V#6 signed off); carve-outs **FRE-646** (extract-tax, needs budget OK) · FRE-490 · FRE-647. FRE-488 was Done-but-broken (harness couldn't measure recall — fixed). **FRE-645 canceled** (dup of FRE-646 + FRE-644). Prior this session: **Config Management & Validation initiative filed**: **FRE-644** ADR [Approved → adr] + **FRE-645** eval-model-config-fidelity [Approved → build; **blocks FRE-491**]. Root finding: config sprawled across `.env`/3 model-YAMLs/governance/compose/container-env with no single source of truth + **undeclared local↔cloud model-role drift** (extraction nano/mini · captains_log+insights nano/sonnet; embedding+reranker consistent). Principle: switch swaps the inference brain only; cognitive-pipeline roles consistent; evals always on prod config; guard the rest. Parameter-manager **UI deferred** (PWA Config Console). Also: read-only Bash perms made permanent; test substrate reset clean+current for the FRE-491 baseline.) 2026-06-27 (master — **ADR-0087 Accepted** (owner; recall-measurement program — reconciled w/ 0098: gate-3 markdown/LLM-wiki *substrate* dropped; Phase-1 tickets **FRE-490/491/493 Approved**, **FRE-494 held [GATED]**). **ADR-0096 Accepted** (owner; memory access model — chain FRE-613–618 still Needs-Approval). Earlier: **owner greenlit the ADR-0098 stream**: ADR-0098 **Accepted**; build wave **FRE-637–642 Approved** (637 head, extraction-first; 642 seam); **FRE-643** Tier-3 deferred-with-trigger; **FRE-467** spatio-temporal location memory Approved + moved to Memory Recall Quality. Prior: PR #263 ADR-0098 authored, #262 FRE-636 spike, #261 ADR-0097). **Header changelog trimmed** — the full historical ship/deploy narrative (back to ~2026-06-07) is archived verbatim in [`completed/2026-06-26-master-header-archive.md`](completed/2026-06-26-master-header-archive.md). This header is **current-state only**; the sections below are the live plan.
