# Evidence-truncation lint allowlist (ADR-0125 D5/AC-5; FRE-1002).
#
# The hook (scripts/check_evidence_truncation.py) flags silent shortening of
# evidence-path content (see the script's module docstring for the exact
# rules). Each entry exempts a single (path, line) violation; only
# (path, line) is matched (kind/reason are documentation).
#
# Schema:
#   - path: str    (relative to repo root, as printed by the lint)
#     line: int     (1-based; must match current source)
#     kind: str     (the violation kind the lint reports)
#     reason: str
#
# Currently EMPTY. The single entry this file shipped with — the task-assist
# render branch's `mem.get('summary', ...)[:150]` in `step_llm_call`, deferred
# by FRE-1002 to FRE-1010 — was retired by FRE-1010, which removed that
# truncation entirely rather than marking it: the rendered value is now bounded
# by `_MAX_ITEM_CHARS` through `mark_truncated`, so there is no silent clip left
# to exempt. An empty allowlist is the intended steady state; an entry here is a
# debt, not a configuration.
[]
