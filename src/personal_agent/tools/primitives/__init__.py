"""Primitive filesystem tools: read and write.

FRE-261 — Step 3: low-level read/write primitives that supersede
``read_file`` and ``write_file`` with richer governance controls
(allowed_paths, forbidden_paths, unattended_paths).
"""
