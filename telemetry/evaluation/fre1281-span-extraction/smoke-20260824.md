# FRE-1281 span extraction — smoke-20260824

- partition: **dev**
- documents: 4
- extractor: span_extraction role (config/model_roles.yaml)
- degraded documents (post-pass failed closed): 1

## Bars

| bar | observed | required | met |
| --- | --- | --- | --- |
| `recall.overall` | 0.917 | >= 0.9 | yes |
| `precision.overall` | 0.846 | >= 0.8 | yes |
| `decomposition.boundary_f1` | 0.880 | >= 0.75 | yes |
| `recall.class.checkable_evaluative` | unmeasured | >= 0.85 | unmeasured |
| `recall.class.dependency_declaration` | unmeasured | >= 0.85 | unmeasured |
| `recall.class.factual_bare_predicate` | 0.833 | >= 0.85 | NO |
| `recall.class.factual_entity` | 1.000 | >= 0.85 | yes |
| `recall.class.nl_in_code` | unmeasured | >= 0.85 | unmeasured |
| `recall.class.prose_about_code` | unmeasured | >= 0.85 | unmeasured |
| `recall.class.prose_in_fence` | unmeasured | >= 0.85 | unmeasured |
| `recall.class.unattributed_restatement` | unmeasured | >= 0.85 | unmeasured |
| `fp_rate.class.attributed_restatement` | unmeasured | <= 0.15 | unmeasured |
| `fp_rate.class.code_body` | unmeasured | <= 0.15 | unmeasured |
| `fp_rate.class.connective_evaluative` | unmeasured | <= 0.15 | unmeasured |
| `fp_rate.class.derived_arithmetic` | unmeasured | <= 0.15 | unmeasured |
| `fp_rate.class.system_record` | unmeasured | <= 0.15 | unmeasured |

**Verdict: FAIL**

Unmet bars, each with the failure it was preregistered to prevent:

- `recall.class.checkable_evaluative` — A class-shaped hole is invisible in an overall figure. checkable_evaluative must clear the bar on its own (ADR-0138 AC-7).
- `recall.class.dependency_declaration` — A class-shaped hole is invisible in an overall figure. dependency_declaration must clear the bar on its own (ADR-0138 AC-7).
- `recall.class.factual_bare_predicate` — A class-shaped hole is invisible in an overall figure. factual_bare_predicate must clear the bar on its own (ADR-0138 AC-7).
- `recall.class.nl_in_code` — A class-shaped hole is invisible in an overall figure. nl_in_code must clear the bar on its own (ADR-0138 AC-7).
- `recall.class.prose_about_code` — A class-shaped hole is invisible in an overall figure. prose_about_code must clear the bar on its own (ADR-0138 AC-7).
- `recall.class.prose_in_fence` — A class-shaped hole is invisible in an overall figure. prose_in_fence must clear the bar on its own (ADR-0138 AC-7).
- `recall.class.unattributed_restatement` — A class-shaped hole is invisible in an overall figure. unattributed_restatement must clear the bar on its own (ADR-0138 AC-7).
- `fp_rate.class.attributed_restatement` — Sweeping attributed_restatement into the contract manufactures refusals the user did not deserve (ADR-0138 AC-3, D7).
- `fp_rate.class.code_body` — Sweeping code_body into the contract manufactures refusals the user did not deserve (ADR-0138 AC-3, D7).
- `fp_rate.class.connective_evaluative` — Sweeping connective_evaluative into the contract manufactures refusals the user did not deserve (ADR-0138 AC-3, D7).
- `fp_rate.class.derived_arithmetic` — Sweeping derived_arithmetic into the contract manufactures refusals the user did not deserve (ADR-0138 AC-3, D7).
- `fp_rate.class.system_record` — Sweeping system_record into the contract manufactures refusals the user did not deserve (ADR-0138 AC-3, D7).

## Per-document

| doc | gold claims | predicted | matched |
| --- | --- | --- | --- |
| d01-tuna-brands | 3 | 4 | 3 |
| d02-mercury-bare | 3 | 3 | 2 |
| d04-city-conjunction | 3 | 3 | 3 |
| d07-bare-predicate-mixed | 3 | 3 | 3 |
