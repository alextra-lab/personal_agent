# ADR-9004: No Status Or Decision Heading

This document has no explicit `**Status:**` marker and no "Decision" heading of any form at all —
just a paragraph of prose describing an outcome, to exercise the fully-degraded fallback path of the
ADR index builder (status reads as unknown, the excerpt falls back to the leading body text).
