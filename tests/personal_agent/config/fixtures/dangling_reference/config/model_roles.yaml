# Fixture: the binding's `deployment` value names a model absent from the
# catalog's models: mapping (AC-9, FRE-650; re-pointed at `bindings:` by ADR-0145 D4).
bindings:
  entity_extraction: { deployment: gpt-9-ghost }
required_secrets:
  local: []
