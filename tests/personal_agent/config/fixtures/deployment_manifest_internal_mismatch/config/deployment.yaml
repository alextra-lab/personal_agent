# Fixture: FRE-651 — this profile row is internally self-contradictory:
# model_config_path and env_overrides.AGENT_MODEL_CONFIG_PATH name different
# files. Isolates check_deployment_manifest_internal_consistency (no compose
# file needed — the check is pure manifest self-consistency).
profiles:
  cloud:
    compose_file: docker-compose.cloud.yml
    model_config_path: config/models.yaml
    env_overrides:
      AGENT_MODEL_CONFIG_PATH: /app/config/models.cloud.yaml
