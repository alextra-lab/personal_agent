# Fixture: FRE-651 — cloud profile's declared env_overrides disagrees with what
# docker-compose.fixture.yml actually sets. Isolates
# check_deployment_manifest_matches_compose (model_config_path here matches
# env_overrides, so check_deployment_manifest_internal_consistency stays clean).
profiles:
  cloud:
    compose_file: docker-compose.fixture.yml
    model_config_path: config/models.WRONG.yaml
    env_overrides:
      AGENT_MODEL_CONFIG_PATH: /app/config/models.WRONG.yaml
