# Fixture compose file: the "real" deployed profile disagrees with the one
# config/deployment.yaml declares this compose file serves (FRE-651).
services:
  seshat-gateway:
    image: fixture
    environment:
      AGENT_DEPLOYMENT_PROFILE: eval
