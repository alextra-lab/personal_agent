# Fixture: definition drift — same matrix key 'gpt-5.4-mini' as cloud, but a
# different underlying id (AC-3 known-bad fixture, stage 2, FRE-650).
models:
  gpt-5.4-mini:
    id: "gpt-4o-mini"
    provider: "openai"
    max_tokens: 4096
