# Fixture (FRE-1443 AC-4): a role-bound cloud producer declaring no reasoning
# depth, reused via `inherit` by a second role.
providers:
  anthropic: { auth_env: anthropic_api_key, placement: cloud, max_concurrency: 50 }
models:
  claude_sonnet:
    id: "claude-sonnet-5"
    provider: "anthropic"
    context_length: 200000
    max_concurrency: 10
    default_timeout: 180
