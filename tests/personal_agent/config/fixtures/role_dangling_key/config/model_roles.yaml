# Fixture: gpt-9-ghost (the binding's `deployment` value) is not a key under
# models: (FRE-650; re-pointed at `bindings:` by ADR-0145 D4).
active_profiles:
  local: config/models.yaml
  cloud: config/models.yaml
bindings:
  entity_extraction: { deployment: gpt-9-ghost }
required_secrets:
  local: []
  cloud: []
