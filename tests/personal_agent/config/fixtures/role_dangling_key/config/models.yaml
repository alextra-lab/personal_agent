# Fixture: gpt-9-ghost (the matrix's `all:` value) is not a key here (FRE-650).
models:
  gpt-5.4-nano:
    id: "gpt-5.4-nano"
    provider: "openai"
    context_length: 128000
    max_concurrency: 10
    default_timeout: 60
