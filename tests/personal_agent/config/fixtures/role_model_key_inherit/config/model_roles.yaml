# Fixture (FRE-1443 AC-3, path 7): a matrix role whose `all:` value is the
# `inherit` sentinel — contrived, since no real role uses this matrix shape
# with `inherit` today, but resolve_role_model_key must resolve it correctly
# once D4 makes it reachable.
roles:
  some_matrix_role: { all: inherit }
required_secrets:
  local: []
bindings: {}
