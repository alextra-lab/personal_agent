# Fixture (ADR-0145 D4): a binding whose `deployment` value is the `inherit`
# sentinel — contrived, since no real role uses `inherit` off the `primary`
# binding today, but resolve_role_model_key must resolve it correctly now
# that it reads `bindings:` directly.
bindings:
  some_matrix_role: { deployment: inherit }
required_secrets:
  local: []
